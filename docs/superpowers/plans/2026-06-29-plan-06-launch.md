# План 6 — Launch (SEO + OG + IndexNow + Метрика + Deploy + Backups + Monitoring)

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. For tasks marked **(TDD)** — use `superpowers:test-driven-development`.

**Goal:** Закрыть Phase 1 — задеплоить проект на Timeweb Cloud VPS под собственный домен с HTTPS (Caddy + Let's Encrypt), наладить индексацию (sitemap, robots, JSON-LD, OG-картинки, IndexNow), Яндекс.Метрику без webvisor, ежедневные бэкапы Postgres в Timeweb S3 Cold (retention 30 дней) и uptime-мониторинг через UptimeRobot + Telegram. Параллельно переименовать env-vars `R2_*` → `STORAGE_*` (легаси-имя сбивает с толку) и вынести контактный email в единую константу `theme/content.ts`.

**Architecture:** Деплой — один прод-VPS (1×5GHz / 1GB / 15GB) с docker-compose (caddy + app + db + backup), ручной `git pull && docker compose up -d`. SEO — App Router Metadata API + три серверных JSON-LD конструктора (`BlogPosting`, `BreadcrumbList`, `WebSite`) в `src/lib/jsonld.ts`. OG = реальный cover-image поста, если есть; иначе динамика через `next/og` (`src/app/og/[slug]/route.tsx`, Node runtime, 1200×630 на градиентном фоне). IndexNow — fire-and-forget `fetch(api.indexnow.org)` с `AbortSignal.timeout(5000)` в хуках `publishPost`/`updatePost`/`adminHidePost`/`adminDeletePost`/`softDeletePost`/`adminBanUser`. Метрика — `<Script strategy="afterInteractive">` в `layout.tsx`, грузится только при `NODE_ENV=production && YANDEX_METRIKA_ID`. Бэкап — отдельный `postgres:16-alpine` контейнер, в entrypoint цикл `sleep до 03:00 MSK → pg_dump | gzip → aws s3 cp → rm`. Без cookie-баннера: футер-disclaimer + страница `/privacy`.

**Tech Stack:** `next/og` (входит в Next.js, без новых deps), `next.MetadataRoute.{Sitemap, Robots}`, `@aws-sdk/client-s3` (уже стоит), `nextjs-toploader` (уже стоит), Caddy v2 (alpine), `postgres:16-alpine` + `aws-cli` (apk add at entrypoint). Никаких новых npm deps. Никаких новых таблиц/миграций.

**Спецификация:** [docs/superpowers/specs/2026-06-29-plan-06-launch-design.md](../specs/2026-06-29-plan-06-launch-design.md) — целиком (§3 routes, §4 подсистемы, §6 env vars, §7 тесты).
**Канон высшего уровня:** [docs/superpowers/specs/2026-06-05-skelet-blog-design.md](../specs/2026-06-05-skelet-blog-design.md) §6 (схема), §11 (деплой/бэкапы), §12.1/§12.2 (что НЕ в фазе 1), §16 (план-06 закрывает MVP).
**Предшественник:** [docs/superpowers/plans/2026-06-21-plan-05b-engagement.md](./2026-06-21-plan-05b-engagement.md) — комменты, модерация, /banned уже на месте.

**Definition of Done (что считается завершением плана 6):**
- `pnpm test` зелёный — минимум **+18 новых** (jsonld 3, indexnow 5, env 2, robots 2, sitemap 4, privacy 1, footer 1). Все ранее проходившие продолжают проходить.
- `pnpm tsc --noEmit` чисто.
- `NODE_ENV=production pnpm build` зелёный.
- `docker compose build app` локально зелёный.
- `docker compose up -d` локально: открываются `/`, `/p/[slug]`, `/t/[slug]`, `/u/[username]`, `/sitemap.xml`, `/robots.txt`, `/api/health`, `/og/[slug]`, `/privacy`.
- `grep -rn "R2_" src/ next.config.ts` — пусто (кроме исторических комментариев о Cloudflare R2 в неактивных файлах, если такие есть).
- JSON-LD на `/p/[slug]` валиден через Google Rich Results Test (manual).
- Caddyfile проходит `docker compose run --rm caddy caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` (или `caddy validate` если установлен локально).
- Recovery drill: тестовый дамп → пустая БД → восстановление → counts совпадают (см. Task 21).
- `docs/DEPLOY.md` написан и проходит manual review.
- `docs/RECOVERY.md` написан и проходит manual review.
- Retro-секция в конце этого файла заполнена расхождениями с планом.

**Сознательно отложено (с маркерами в коде / эпилоге):**
- **Реальный деплой на VPS** — выходит за рамки plan-06 как такового (план готовит код+docs+конфиги; нажать `git pull && docker compose up -d` пользователь сделает руками после prowadzenia DoD-чеклиста).
- **Регистрация в GSC / Webmaster / Metrika / UptimeRobot** — manual post-deploy шаги, документируются в `docs/DEPLOY.md` §9 spec'а; не входят в код-DoD плана.
- **Поиск (tsvector), RSS, лайки, треды, подписки, email-логин** — Phase 2/3 (§12 канона).
- **Staging-окружение, CI/CD** — Phase 2 (когда появится регулярный поток изменений).
- **Webvisor, persistent observability** — Phase 2/3 (см. spec §1).
- **CDN перед S3** — когда появится трафик.
- **delete-account UI** — Phase 2; в `/privacy` указываем email для запроса вручную.
- **Миграция dev-картинок Yandex → Timeweb** — прод стартует с пустым bucket; локально остаётся текущий Yandex до момента «надоело».

---

## Repo layout, который добавляем/меняем в этом плане

```
skelet/
├── README.md                                       # ← короткая заметка про launch + retro (Task 22)
├── Caddyfile                                       # ← MODIFY: LETSENCRYPT_EMAIL глобально, www→apex redir, ++security headers, /og/* в @static
├── docker-compose.yml                              # ← MODIFY: app healthcheck, backup entrypoint, LETSENCRYPT_EMAIL для caddy
├── .env.example                                    # ← MODIFY: STORAGE_*, BACKUP_S3_*, DOMAIN, LETSENCRYPT_EMAIL, INDEXNOW_KEY, YANDEX_METRIKA_ID
├── next.config.ts                                  # ← MODIFY: r2Public → storagePublic
│
├── docs/
│   ├── DEPLOY.md                                   # ← NEW: пошагово от пустого VPS до работающего сайта
│   └── RECOVERY.md                                 # ← NEW: восстановление из бэкапа
│
├── scripts/
│   ├── backup.sh                                   # ← REPLACE: финальная версия (pg_dump | gzip | aws s3 cp)
│   └── backup-entrypoint.sh                        # ← NEW: sleep до 03:00 MSK, цикл вызовов backup.sh
│
├── public/
│   └── (verification-файлы добавляются вручную post-deploy — см. spec §3.5, не commit'им в plan-06)
│
├── theme/
│   └── content.ts                                  # ← MODIFY: SITE_CONTACT_EMAIL константа, site.contactEmail, privacy.*, footer.disclaimer/privacyLink
│
├── src/
│   ├── lib/
│   │   ├── env.ts                                  # ← MODIFY: R2_* → STORAGE_*, +BACKUP_S3_*, +DOMAIN, +LETSENCRYPT_EMAIL, +INDEXNOW_KEY, +YANDEX_METRIKA_ID
│   │   ├── jsonld.ts                               # ← NEW: buildBlogPostingJsonLd / buildBreadcrumbJsonLd / buildWebSiteJsonLd
│   │   ├── indexnow.ts                             # ← NEW: pingIndexNow + postUrlsForIndexNow
│   │   └── storage/
│   │       ├── r2.ts → storage/s3.ts               # ← RENAME + MODIFY: STORAGE_*-переменные (или оставить файл r2.ts, экспорты переименовать)
│   │       └── upload.ts                           # ← MODIFY: STORAGE_PUBLIC_BASE
│   │
│   ├── components/
│   │   ├── analytics/
│   │   │   └── YandexMetrika.tsx                   # ← NEW: <Script afterInteractive> + <noscript> pixel
│   │   ├── layout/
│   │   │   └── Footer.tsx                          # ← MODIFY: + disclaimer + ссылка на /privacy
│   │   └── seo/
│   │       └── JsonLd.tsx                          # ← NEW (опц., микро-обёртка <script type="application/ld+json"...>)
│   │
│   ├── server/
│   │   ├── posts.ts                                # ← MODIFY: pingIndexNow в publishPost/republishPost/softDeletePost
│   │   └── actions/
│   │       └── moderation.ts                       # ← MODIFY: pingIndexNow в adminHidePost/adminDeletePost/adminBanUser
│   │
│   ├── app/
│   │   ├── layout.tsx                              # ← MODIFY: YandexMetrika в production
│   │   ├── sitemap.ts                              # ← MODIFY: убрать siteConfig.url, через getEnv().NEXTAUTH_URL (или оставить как есть — это решение в Task 9)
│   │   ├── robots.ts                               # ← NEW: disallow приватных, sitemap URL
│   │   ├── api/
│   │   │   ├── health/route.ts                     # ← (уже есть, проверяем что 200/503 корректно)
│   │   │   └── upload/route.ts                     # ← MODIFY: env.R2_* → env.STORAGE_*
│   │   ├── og/
│   │   │   └── [slug]/route.tsx                    # ← NEW: ImageResponse 1200×630
│   │   ├── (public)/
│   │   │   ├── privacy/page.tsx                    # ← NEW: серверная политика конфиденциальности
│   │   │   ├── p/[slug]/page.tsx                   # ← MODIFY: JSON-LD BlogPosting+Breadcrumb, OG image выбор
│   │   │   └── (feed)/
│   │   │       ├── page.tsx                        # ← MODIFY: JSON-LD WebSite, generateMetadata (если ещё нет)
│   │   │       ├── t/[slug]/page.tsx               # ← MODIFY: JSON-LD Breadcrumb
│   │   │       └── u/[username]/page.tsx           # ← MODIFY: JSON-LD Breadcrumb
│   │   ├── (app)/
│   │   │   ├── new/page.tsx                        # ← MODIFY: robots noindex (если не стоит)
│   │   │   └── edit/[id]/page.tsx                  # ← MODIFY: robots noindex
│   │   └── banned/page.tsx                         # ← MODIFY: contact через content.site.contactEmail (если уже не так)
│   │
│   └── (feed)/drafts/page.tsx                      # ← если есть — robots noindex
│
└── tests/
    ├── lib/
    │   ├── jsonld.test.ts                          # 3 кейса
    │   ├── indexnow.test.ts                        # 5 кейсов (vi.spyOn fetch)
    │   └── env-production.test.ts                  # 2 кейса (parseEnv в production без STORAGE_* падает)
    ├── seo/
    │   ├── robots.test.ts                          # 2 кейса
    │   └── sitemap.test.ts                         # 4 кейса (integration на test-DB)
    ├── components/
    │   ├── analytics/
    │   │   └── YandexMetrika.test.tsx              # 1 кейс
    │   └── layout/
    │       └── Footer.test.tsx                     # 1 кейс
    └── app/
        └── privacy.test.tsx                        # 1 кейс (smoke)
```

---

## Task 1: Env vars refactor — `R2_*` → `STORAGE_*` + новые переменные **(rename-only commit)**

**Files:**
- Modify: `src/lib/env.ts`
- Modify: `src/lib/storage/r2.ts` (содержимое; имя файла оставляем `r2.ts` чтобы не плодить переименований импортёров — экспортируемые функции `getR2Client` и `r2Bucket` тоже оставляем как есть, см. Step 1.4 ниже)
- Modify: `src/lib/storage/upload.ts`
- Modify: `src/app/api/upload/route.ts`
- Modify: `next.config.ts`
- Modify: `.env`, `.env.example`

Спецификация: §4.6 + §6.

Стратегия: **переименование env-переменных и Zod-полей**, при этом **внутренние идентификаторы кода** (`getR2Client`, `r2Bucket`, `r2Public` в next.config) оставляем — это сводит scope коммита к строкам с env-vars. Маркер для будущего: переименовать сами идентификаторы в отдельном chore (Phase 2+).

- [ ] **Step 1.1: Расширить Zod-схему `src/lib/env.ts`**

Открой `src/lib/env.ts`. Полностью заменить файл на:

```ts
import { z } from "zod";

const schema = z.object({
  NODE_ENV: z.enum(["development", "test", "production"]).default("development"),
  DATABASE_URL: z.string().url().refine(s => s.startsWith("postgres://") || s.startsWith("postgresql://"), {
    message: "DATABASE_URL must be a Postgres connection string",
  }),
  NEXTAUTH_URL: z.string().url(),
  NEXTAUTH_SECRET: z.string().min(32, "NEXTAUTH_SECRET must be ≥32 chars"),

  // Production-only (валидируется в superRefine ниже)
  DOMAIN: z.string().min(1).optional(),
  LETSENCRYPT_EMAIL: z.string().email().optional(),

  YANDEX_CLIENT_ID: z.string().min(1).optional(),
  YANDEX_CLIENT_SECRET: z.string().min(1).optional(),
  VK_CLIENT_ID: z.string().min(1).optional(),
  VK_CLIENT_SECRET: z.string().min(1).optional(),

  STORAGE_ENDPOINT:          z.string().url().optional(),
  STORAGE_BUCKET:            z.string().min(1).optional(),
  STORAGE_ACCESS_KEY_ID:     z.string().min(1).optional(),
  STORAGE_SECRET_ACCESS_KEY: z.string().min(1).optional(),
  STORAGE_PUBLIC_BASE:       z.string().url().optional(),

  BACKUP_S3_ENDPOINT:          z.string().url().optional(),
  BACKUP_S3_BUCKET:            z.string().min(1).optional(),
  BACKUP_S3_ACCESS_KEY_ID:     z.string().min(1).optional(),
  BACKUP_S3_SECRET_ACCESS_KEY: z.string().min(1).optional(),

  INDEXNOW_KEY: z.string().regex(/^[a-f0-9]{8,128}$/, "INDEXNOW_KEY must be hex").optional(),
  YANDEX_METRIKA_ID: z.string().regex(/^\d+$/, "YANDEX_METRIKA_ID must be digits").optional(),
}).superRefine((v, ctx) => {
  // Парные ID/SECRET для OAuth провайдеров
  for (const p of ["YANDEX", "VK"] as const) {
    const id = (v as Record<string, string | undefined>)[`${p}_CLIENT_ID`];
    const sec = (v as Record<string, string | undefined>)[`${p}_CLIENT_SECRET`];
    if (!!id !== !!sec) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: [`${p}_CLIENT_SECRET`],
        message: `${p}_CLIENT_ID and ${p}_CLIENT_SECRET must be set together`,
      });
    }
  }

  // STORAGE_* — все или ничего (как раньше для R2_*)
  const storageKeys = [
    "STORAGE_ENDPOINT", "STORAGE_BUCKET", "STORAGE_ACCESS_KEY_ID",
    "STORAGE_SECRET_ACCESS_KEY", "STORAGE_PUBLIC_BASE",
  ] as const;
  const storagePresence = storageKeys.map(k => Boolean((v as Record<string, string | undefined>)[k]));
  const storageAll = storagePresence.every(Boolean);
  const storageNone = storagePresence.every(p => !p);
  if (!storageAll && !storageNone) {
    ctx.addIssue({
      code: z.ZodIssueCode.custom,
      path: ["STORAGE_BUCKET"],
      message: "STORAGE_* env vars must be all set or all empty",
    });
  }

  // BACKUP_S3_* — все или ничего
  const backupKeys = [
    "BACKUP_S3_ENDPOINT", "BACKUP_S3_BUCKET",
    "BACKUP_S3_ACCESS_KEY_ID", "BACKUP_S3_SECRET_ACCESS_KEY",
  ] as const;
  const backupPresence = backupKeys.map(k => Boolean((v as Record<string, string | undefined>)[k]));
  const backupAll = backupPresence.every(Boolean);
  const backupNone = backupPresence.every(p => !p);
  if (!backupAll && !backupNone) {
    ctx.addIssue({
      code: z.ZodIssueCode.custom,
      path: ["BACKUP_S3_BUCKET"],
      message: "BACKUP_S3_* env vars must be all set or all empty",
    });
  }

  // В production требуем полный набор для деплоя
  if (v.NODE_ENV === "production") {
    const required: Array<keyof typeof v> = ["DOMAIN", "LETSENCRYPT_EMAIL"];
    for (const k of required) {
      if (!v[k]) {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          path: [k as string],
          message: `${String(k)} is required in production`,
        });
      }
    }
    if (!storageAll) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: ["STORAGE_BUCKET"],
        message: "STORAGE_* must be fully configured in production",
      });
    }
  }
});

export type Env = z.infer<typeof schema>;

export function parseEnv(input: Record<string, string | undefined>): Env {
  const result = schema.safeParse(input);
  if (!result.success) {
    throw new Error("Invalid env: " + result.error.issues.map(i => `${i.path.join(".")}: ${i.message}`).join("; "));
  }
  return result.data;
}

let cached: Env | null = null;
export function getEnv(): Env {
  if (cached) return cached;
  cached = parseEnv(process.env);
  return cached;
}

export function _resetEnvCacheForTests(): void {
  cached = null;
}
```

Изменения относительно текущего файла:
- `R2_*` → `STORAGE_*`
- добавлены `DOMAIN`, `LETSENCRYPT_EMAIL`, `BACKUP_S3_*`, `INDEXNOW_KEY`, `YANDEX_METRIKA_ID`
- добавлен `superRefine` блок для production-режима

- [ ] **Step 1.2: Обновить storage call-sites — `src/lib/storage/r2.ts`**

В `src/lib/storage/r2.ts` поменять только обращения к env. Тело файла:

```ts
import { S3Client } from "@aws-sdk/client-s3";
import { getEnv } from "@/lib/env";

let _client: S3Client | null = null;

// Имя getR2Client/r2Bucket историческое (изначально планировалась Cloudflare R2);
// фактически используется Timeweb S3. Переименование идентификаторов — отдельный chore.
export function getR2Client(): S3Client {
  if (_client) return _client;
  const env = getEnv();
  if (!env.STORAGE_ENDPOINT || !env.STORAGE_ACCESS_KEY_ID || !env.STORAGE_SECRET_ACCESS_KEY) {
    throw new Error("Storage not configured");
  }
  _client = new S3Client({
    region: "ru-central1",
    endpoint: env.STORAGE_ENDPOINT,
    credentials: {
      accessKeyId: env.STORAGE_ACCESS_KEY_ID,
      secretAccessKey: env.STORAGE_SECRET_ACCESS_KEY,
    },
  });
  return _client;
}

export function r2Bucket(): string {
  const env = getEnv();
  if (!env.STORAGE_BUCKET) throw new Error("STORAGE_BUCKET not set");
  return env.STORAGE_BUCKET;
}

export function _resetR2ClientForTests(): void {
  _client = null;
}
```

- [ ] **Step 1.3: `src/lib/storage/upload.ts`**

Заменить body `buildPublicUrl`:

```ts
export function buildPublicUrl(key: string): string {
  const env = getEnv();
  if (!env.STORAGE_PUBLIC_BASE) throw new Error("STORAGE_PUBLIC_BASE not set");
  const base = env.STORAGE_PUBLIC_BASE.replace(/\/$/, "");
  return `${base}/${key}`;
}
```

- [ ] **Step 1.4: `src/app/api/upload/route.ts`**

Открой файл, найди строку `if (!env.R2_ENDPOINT) return errJson(503, "storage_not_configured");` и замени на:

```ts
if (!env.STORAGE_ENDPOINT) return errJson(503, "storage_not_configured");
```

- [ ] **Step 1.5: `next.config.ts`**

Заменить `const r2Public = process.env.R2_PUBLIC_BASE;` на `const storagePublic = process.env.STORAGE_PUBLIC_BASE;`. Соответственно поменять `if (r2Public)` → `if (storagePublic)` и `new URL(r2Public)` → `new URL(storagePublic)`. Комментарий `// Игнорим невалидный R2_PUBLIC_BASE` → `// Игнорим невалидный STORAGE_PUBLIC_BASE`.

- [ ] **Step 1.6: `.env` (локальный, gitignored) и `.env.example`**

В обоих файлах:
- Переименовать все `R2_*` строки в `STORAGE_*` (значения оставить).
- Добавить (с пустыми значениями для example):

```bash
DOMAIN=
LETSENCRYPT_EMAIL=

BACKUP_S3_ENDPOINT=
BACKUP_S3_BUCKET=
BACKUP_S3_ACCESS_KEY_ID=
BACKUP_S3_SECRET_ACCESS_KEY=

INDEXNOW_KEY=
YANDEX_METRIKA_ID=
```

В локальном `.env` оставь старые значения для текущего Yandex Storage (в полях STORAGE_*) — на dev продолжаешь работать с ним. Полный пример финального `.env.example` — см. spec §6.

- [ ] **Step 1.7: Проверка чистоты — никаких R2_ в коде**

```bash
grep -rn "R2_" src/ next.config.ts || echo "OK — no R2_ left"
grep -rn "STORAGE_" src/ next.config.ts | head
```

Ожидание: первая команда — `OK — no R2_ left`. Вторая — список 5–6 совпадений (в `env.ts`, `r2.ts`, `upload.ts`, `upload/route.ts`, `next.config.ts`).

Замечание: если `grep -rn "R2_"` находит совпадения в комментариях/строках типа `// R2 client...` — оставляем (это исторический контекст, не функциональный код).

- [ ] **Step 1.8: tsc + smoke**

```bash
pnpm tsc --noEmit
pnpm test tests/api/upload.test.ts tests/lib/env.test.ts 2>/dev/null || pnpm test
```

Ожидание: tsc чисто. Если какие-то существующие тесты падают из-за `R2_*` — найди их и переименуй (`grep -rn "R2_" tests/`).

- [ ] **Step 1.9: Коммит**

```bash
git add src/lib/env.ts src/lib/storage/ src/app/api/upload/route.ts next.config.ts .env.example
# .env — gitignored, не коммитим
git commit -m "refactor(plan-6): rename R2_* env vars to STORAGE_*, add BACKUP_S3_*, DOMAIN, LETSENCRYPT_EMAIL, INDEXNOW_KEY, YANDEX_METRIKA_ID"
```

---

## Task 2: Env production-режим тесты **(TDD)**

**Files:**
- Create: `tests/lib/env-production.test.ts`

Spec §6: zod-схема должна валить prod-окружение без `STORAGE_*` / `DOMAIN` / `LETSENCRYPT_EMAIL`, и пропускать те же значения в dev.

- [ ] **Step 2.1: (TDD RED) Написать тесты**

```ts
// tests/lib/env-production.test.ts
import { describe, it, expect } from "vitest";
import { parseEnv } from "@/lib/env";

const BASE: Record<string, string> = {
  DATABASE_URL: "postgres://app:pw@db:5432/app",
  NEXTAUTH_URL: "https://example.ru",
  NEXTAUTH_SECRET: "x".repeat(32),
};

describe("parseEnv — production gate", () => {
  it("прод без DOMAIN/LETSENCRYPT_EMAIL — падает", () => {
    expect(() => parseEnv({ ...BASE, NODE_ENV: "production" }))
      .toThrow(/DOMAIN is required in production/);
  });

  it("прод без STORAGE_* — падает", () => {
    expect(() => parseEnv({
      ...BASE,
      NODE_ENV: "production",
      DOMAIN: "example.ru",
      LETSENCRYPT_EMAIL: "ops@example.ru",
    })).toThrow(/STORAGE_/);
  });
});
```

- [ ] **Step 2.2: (RED) Прогнать — должны падать ровно с этими сообщениями**

```bash
pnpm test tests/lib/env-production.test.ts
```

Ожидание: 2/2 PASS (Task 1 уже добавил production-gate в superRefine, так что красным тут будет только если что-то не доделано в Task 1).

Если падают по другой причине (например `STORAGE_* env vars must be all set or all empty` вместо `STORAGE_* must be fully configured in production`) — сверь сообщения с superRefine в Task 1.1 и поправь.

- [ ] **Step 2.3: Коммит**

```bash
git add tests/lib/env-production.test.ts
git commit -m "test(plan-6): parseEnv guard for production env vars"
```

---

## Task 3: Контактный email — `SITE_CONTACT_EMAIL` константа в `theme/content.ts`

**Files:**
- Modify: `theme/content.ts`

Spec §4.7. Цель: вынести email в одну константу, все строки строятся через template literal с её ссылкой; никаких runtime-helper'ов, никаких placeholder-токенов.

- [ ] **Step 3.1: Прочесть текущее состояние `theme/content.ts`**

Открой файл. Тебе нужны блоки: `site`, `banned.contact`. Заметь, что в текущем коде email хардкоднут как `"test@mail.ru"` внутри `banned.contact`.

- [ ] **Step 3.2: Добавить константу и расширить блоки `site` / `banned` / `privacy` / `footer`**

В самом верху файла (перед `export const content = {`) добавь:

```ts
const SITE_CONTACT_EMAIL = "test@mail.ru";
```

В блоке `site` добавь поле `contactEmail`:

```ts
site: {
  name: "Skelet",
  shortName: "Skelet",
  tagline: "Сообщество (тестовая ниша)",
  description: "Тестовый инстанс скелета. Замените текстами своей ниши в theme/content.ts.",
  contactEmail: SITE_CONTACT_EMAIL,
},
```

В блоке `banned.contact` заменить строку на интерполяцию:

```ts
banned: {
  heading: "Ваша учётная запись заблокирована",
  reasonLabel: "Причина:",
  noReason: "Причина не указана.",
  contact: `Для подробной информации напишите: ${SITE_CONTACT_EMAIL}`,
  logout: "Выйти",
},
```

В `footer` — добавить два поля для disclaimer и ссылки на /privacy (не трогая существующие `about`/`rules`/`contacts`):

```ts
footer: {
  about: "О проекте",
  rules: "Правила",
  contacts: "Контакты",
  disclaimer: "Используем cookies и Yandex.Metrika для аналитики.",
  privacyLink: "Политика",
},
```

Добавить новый блок `privacy` (рядом с `banned`):

```ts
privacy: {
  title: "Политика конфиденциальности",
  intro: "Этот сайт — небольшой блог на платформе Skelet. Ниже описано, какие данные собираем и зачем.",
  section: {
    whoWeAre: "Кто мы",
    whoWeAreBody: `Skelet — независимый проект. По вопросам обработки персональных данных пишите на ${SITE_CONTACT_EMAIL}.`,
    whatWeCollect: "Какие данные собираем",
    whatWeCollectBody: "Email и публичный профиль (имя, никнейм, аватар) при входе через OAuth-провайдеры (Yandex, VK, Google, GitHub). Контент, который вы публикуете (посты, комментарии, темы). Технические данные через Yandex.Metrika — IP, User-Agent, путь, реферер, длительность сессии (без webvisor).",
    cookies: "Cookies",
    cookiesBody: "Используем cookies для авторизации (next-auth) и Yandex.Metrika (anonymous-ID, рекламные cookies не ставим).",
    delete: "Как удалить аккаунт",
    deleteBody: `Чтобы удалить аккаунт, напишите на ${SITE_CONTACT_EMAIL} — удалим в течение 7 дней.`,
    contact: "Контакты",
  },
  contact: `По вопросам обработки данных пишите: ${SITE_CONTACT_EMAIL}`,
  updatedAt: "Обновлено: 2026-06-29",
},
```

- [ ] **Step 3.3: Smoke — tsc + check-theme**

```bash
pnpm tsc --noEmit
pnpm check-theme
```

Ожидание: оба зелёные. `check-theme` — внутренний скрипт, проверяющий целостность theme-объекта (см. `scripts/check-theme.ts`). Если он жалуется на отсутствие новых ключей в `tokens.schema.md` — добавь их туда (одна строка на ключ).

- [ ] **Step 3.4: Banned-страница уже использует `content.banned.contact`** — проверка

```bash
grep -n "banned.contact" src/app/banned/page.tsx
```

Ожидание: одна строка с `{content.banned.contact}`. Никаких правок не нужно — теперь там email через константу.

- [ ] **Step 3.5: Коммит**

```bash
git add theme/content.ts theme/tokens.schema.md
git commit -m "feat(plan-6): SITE_CONTACT_EMAIL constant + privacy/footer content keys"
```

---

## Task 4: JSON-LD конструкторы — `src/lib/jsonld.ts` **(TDD)**

**Files:**
- Create: `src/lib/jsonld.ts`
- Create: `tests/lib/jsonld.test.ts`

Spec §4.1. Pure-функции без зависимостей. Принимают данные, возвращают plain object (потом сериализуется в `<script>` через `JSON.stringify`).

- [ ] **Step 4.1: (TDD RED) Тесты**

```ts
// tests/lib/jsonld.test.ts
import { describe, it, expect } from "vitest";
import { buildBlogPostingJsonLd, buildBreadcrumbJsonLd, buildWebSiteJsonLd } from "@/lib/jsonld";

describe("buildBlogPostingJsonLd", () => {
  it("содержит обязательные поля schema.org BlogPosting", () => {
    const ld = buildBlogPostingJsonLd({
      post: {
        slug: "test-post",
        title: "Тестовый пост",
        excerpt: "Краткое описание",
        pubAt: new Date("2026-06-29T10:00:00Z"),
        updatedAt: new Date("2026-06-30T12:00:00Z"),
        coverUrl: "https://example.ru/cover.webp",
        contentHtml: "<p>Body</p>",
      },
      author: { username: "alice", name: "Алиса" },
      tags: [{ name: "Тест" }, { name: "Skelet" }],
      siteUrl: "https://example.ru",
    });
    expect(ld["@context"]).toBe("https://schema.org");
    expect(ld["@type"]).toBe("BlogPosting");
    expect(ld.headline).toBe("Тестовый пост");
    expect(ld.url).toBe("https://example.ru/p/test-post");
    expect(ld.datePublished).toBe("2026-06-29T10:00:00.000Z");
    expect(ld.dateModified).toBe("2026-06-30T12:00:00.000Z");
    expect(ld.author).toMatchObject({ "@type": "Person", name: "Алиса", url: "https://example.ru/u/alice" });
    expect(ld.image).toBe("https://example.ru/cover.webp");
    expect(ld.keywords).toBe("Тест, Skelet");
  });

  it("если coverUrl отсутствует — использует динамический OG URL", () => {
    const ld = buildBlogPostingJsonLd({
      post: {
        slug: "no-cover", title: "X", excerpt: "", pubAt: new Date(),
        updatedAt: new Date(), coverUrl: null, contentHtml: "",
      },
      author: { username: null, name: null },
      tags: [],
      siteUrl: "https://example.ru",
    });
    expect(ld.image).toBe("https://example.ru/og/no-cover");
    expect(ld.author.name).toBe("Аноним");
  });
});

describe("buildBreadcrumbJsonLd", () => {
  it("корректный itemListElement для трёх уровней", () => {
    const ld = buildBreadcrumbJsonLd([
      { name: "Главная", url: "https://example.ru/" },
      { name: "Темы", url: "https://example.ru/tags" },
      { name: "JS", url: "https://example.ru/t/js" },
    ]);
    expect(ld["@type"]).toBe("BreadcrumbList");
    expect(ld.itemListElement).toHaveLength(3);
    expect(ld.itemListElement[0]).toMatchObject({ "@type": "ListItem", position: 1, name: "Главная", item: "https://example.ru/" });
    expect(ld.itemListElement[2].position).toBe(3);
  });
});

describe("buildWebSiteJsonLd", () => {
  it("содержит url, name, inLanguage", () => {
    const ld = buildWebSiteJsonLd("https://example.ru");
    expect(ld["@type"]).toBe("WebSite");
    expect(ld.url).toBe("https://example.ru/");
    expect(ld.inLanguage).toBe("ru-RU");
  });
});
```

- [ ] **Step 4.2: (RED) Запустить — все 4 должны падать**

```bash
pnpm test tests/lib/jsonld.test.ts
```

Ожидание: FAIL (модуль не существует).

- [ ] **Step 4.3: Реализовать `src/lib/jsonld.ts`**

```ts
// src/lib/jsonld.ts
// Конструкторы schema.org объектов для встраивания через
// <script type="application/ld+json"> на публичных страницах.
// Возвращают plain objects; сериализуются вызывающим через JSON.stringify.

export interface JsonLd {
  "@context": "https://schema.org";
  "@type": string;
  // Индекс намеренно `any`: schema.org-объекты имеют разную форму
  // (BlogPosting / BreadcrumbList / WebSite), а тесты ходят по полям
  // напрямую (ld.author.name, ld.itemListElement[0].position).
  [k: string]: any;
}

const stripSlash = (s: string) => s.replace(/\/$/, "");

export function buildBlogPostingJsonLd(input: {
  post: {
    slug: string;
    title: string;
    excerpt: string;
    pubAt: Date | null;
    updatedAt: Date | null;
    coverUrl: string | null;
    contentHtml: string;
  };
  author: { username: string | null; name: string | null };
  tags: { name: string }[];
  siteUrl: string;
}): JsonLd {
  const base = stripSlash(input.siteUrl);
  const postUrl = `${base}/p/${input.post.slug}`;
  const authorName = input.author.name ?? input.author.username ?? "Аноним";
  const authorUrl = input.author.username ? `${base}/u/${input.author.username}` : undefined;
  const image = input.post.coverUrl ?? `${base}/og/${input.post.slug}`;
  const datePublished = (input.post.pubAt ?? new Date()).toISOString();
  const dateModified = (input.post.updatedAt ?? input.post.pubAt ?? new Date()).toISOString();
  const keywords = input.tags.map(t => t.name).join(", ");

  return {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: input.post.title,
    description: input.post.excerpt,
    url: postUrl,
    mainEntityOfPage: postUrl,
    datePublished,
    dateModified,
    image,
    author: { "@type": "Person", name: authorName, ...(authorUrl ? { url: authorUrl } : {}) },
    inLanguage: "ru-RU",
    ...(keywords ? { keywords } : {}),
  };
}

export function buildBreadcrumbJsonLd(items: { name: string; url: string }[]): JsonLd {
  return {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: items.map((it, i) => ({
      "@type": "ListItem",
      position: i + 1,
      name: it.name,
      item: it.url,
    })),
  };
}

export function buildWebSiteJsonLd(siteUrl: string): JsonLd {
  const base = `${stripSlash(siteUrl)}/`;
  return {
    "@context": "https://schema.org",
    "@type": "WebSite",
    url: base,
    name: "Skelet",
    inLanguage: "ru-RU",
  };
}
```

- [ ] **Step 4.4: (GREEN) Прогнать — все 4 PASS**

```bash
pnpm test tests/lib/jsonld.test.ts
```

Ожидание: 4/4 PASS.

- [ ] **Step 4.5: Коммит**

```bash
git add src/lib/jsonld.ts tests/lib/jsonld.test.ts
git commit -m "feat(plan-6): JSON-LD constructors (BlogPosting, BreadcrumbList, WebSite)"
```

---

## Task 5: IndexNow lib — `src/lib/indexnow.ts` **(TDD)**

**Files:**
- Create: `src/lib/indexnow.ts`
- Create: `tests/lib/indexnow.test.ts`

Spec §4.3. Pure-функция + side-effect функция (fetch), но всё mock'аем через `vi.spyOn(global, "fetch")`.

- [ ] **Step 5.1: (TDD RED) Тесты**

```ts
// tests/lib/indexnow.test.ts
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { pingIndexNow, postUrlsForIndexNow } from "@/lib/indexnow";
import { _resetEnvCacheForTests } from "@/lib/env";

const ORIGINAL_NODE_ENV = process.env.NODE_ENV;
const ORIGINAL_INDEXNOW_KEY = process.env.INDEXNOW_KEY;

describe("pingIndexNow", () => {
  beforeEach(() => {
    _resetEnvCacheForTests();
    vi.restoreAllMocks();
  });
  afterEach(() => {
    process.env.NODE_ENV = ORIGINAL_NODE_ENV;
    process.env.INDEXNOW_KEY = ORIGINAL_INDEXNOW_KEY;
    _resetEnvCacheForTests();
  });

  it("в NODE_ENV=development — fetch не вызывается", async () => {
    process.env.NODE_ENV = "development";
    process.env.INDEXNOW_KEY = "abc123";
    const fetchSpy = vi.spyOn(global, "fetch").mockResolvedValue(new Response("", { status: 200 }));
    await pingIndexNow(["https://example.ru/p/x"]);
    expect(fetchSpy).not.toHaveBeenCalled();
  });

  it("в production без INDEXNOW_KEY — fetch не вызывается", async () => {
    process.env.NODE_ENV = "production";
    delete process.env.INDEXNOW_KEY;
    const fetchSpy = vi.spyOn(global, "fetch").mockResolvedValue(new Response("", { status: 200 }));
    await pingIndexNow(["https://example.ru/p/x"]);
    expect(fetchSpy).not.toHaveBeenCalled();
  });

  it("пустой urls — fetch не вызывается", async () => {
    process.env.NODE_ENV = "production";
    process.env.INDEXNOW_KEY = "abc123";
    const fetchSpy = vi.spyOn(global, "fetch").mockResolvedValue(new Response("", { status: 200 }));
    await pingIndexNow([]);
    expect(fetchSpy).not.toHaveBeenCalled();
  });

  it("production + key + urls — POST на api.indexnow.org с правильным body", async () => {
    process.env.NODE_ENV = "production";
    process.env.INDEXNOW_KEY = "abc123";
    const fetchSpy = vi.spyOn(global, "fetch").mockResolvedValue(new Response("", { status: 200 }));
    await pingIndexNow(["https://example.ru/p/x", "https://example.ru/"]);
    expect(fetchSpy).toHaveBeenCalledTimes(1);
    const [url, init] = fetchSpy.mock.calls[0];
    expect(url).toBe("https://api.indexnow.org/indexnow");
    expect((init as RequestInit).method).toBe("POST");
    const body = JSON.parse((init as RequestInit).body as string);
    expect(body.host).toBe("example.ru");
    expect(body.key).toBe("abc123");
    expect(body.keyLocation).toBe("https://example.ru/abc123.txt");
    expect(body.urlList).toEqual(["https://example.ru/p/x", "https://example.ru/"]);
  });

  it("fetch падает — не пробрасывает ошибку (fire-and-forget)", async () => {
    process.env.NODE_ENV = "production";
    process.env.INDEXNOW_KEY = "abc123";
    vi.spyOn(global, "fetch").mockRejectedValue(new Error("network down"));
    await expect(pingIndexNow(["https://example.ru/x"])).resolves.toBeUndefined();
  });
});

describe("postUrlsForIndexNow", () => {
  it("включает post URL, главную, теги, и автора если есть username", () => {
    const urls = postUrlsForIndexNow({
      siteUrl: "https://example.ru/",   // trailing slash сознательно — должен стрипнуться
      postSlug: "hello",
      authorUsername: "alice",
      tagSlugs: ["js", "rust"],
    });
    expect(urls).toEqual([
      "https://example.ru/p/hello",
      "https://example.ru/",
      "https://example.ru/t/js",
      "https://example.ru/t/rust",
      "https://example.ru/u/alice",
    ]);
  });
});
```

Замечание про NODE_ENV/key в тестах: pingIndexNow читает `NEXTAUTH_URL` через `getEnv()` — поэтому в `vitest.setup.ts` уже должен быть выставлен валидный `NEXTAUTH_URL`. Проверь существующий `vitest.config.ts`/setup: если `NEXTAUTH_URL` подставляется через `dotenv -c test` или хардкодным значением — этого хватит. Если нет — добавь в setup-файл:

```ts
process.env.NEXTAUTH_URL ??= "https://example.ru";
process.env.NEXTAUTH_SECRET ??= "x".repeat(32);
process.env.DATABASE_URL ??= "postgres://app:pw@localhost:5432/app";
```

- [ ] **Step 5.2: (RED) Прогнать тесты**

```bash
pnpm test tests/lib/indexnow.test.ts
```

Ожидание: FAIL (модуль не существует).

- [ ] **Step 5.3: Реализовать `src/lib/indexnow.ts`**

Spec §4.3 — копируем код целиком:

```ts
// src/lib/indexnow.ts
import { getEnv } from "@/lib/env";

const INDEXNOW_ENDPOINT = "https://api.indexnow.org/indexnow";
const INDEXNOW_URLLIST_LIMIT = 10_000; // лимит протокола

// Fire-and-forget пинг IndexNow API.
// В dev/test или без INDEXNOW_KEY — no-op. Ошибки fetch'а ловит, не пробрасывает —
// чтобы провал внешнего сервиса не валил server action публикации поста.
export async function pingIndexNow(urls: string[]): Promise<void> {
  if (process.env.NODE_ENV !== "production") return;
  const key = process.env.INDEXNOW_KEY;
  if (!key) return;
  if (urls.length === 0) return;

  const siteUrl = getEnv().NEXTAUTH_URL.replace(/\/$/, "");
  const host = new URL(siteUrl).host;

  try {
    await fetch(INDEXNOW_ENDPOINT, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        host,
        key,
        keyLocation: `${siteUrl}/${key}.txt`,
        urlList: urls.slice(0, INDEXNOW_URLLIST_LIMIT),
      }),
      signal: AbortSignal.timeout(5000), // 5 секунд достаточно
    });
  } catch (e) {
    console.warn("[indexnow] ping failed", e);
  }
}

// Хелпер: вычислить набор URL'ов для пинга при изменении конкретного поста.
// Не делает side-эффектов; pure.
export function postUrlsForIndexNow(args: {
  siteUrl: string;
  postSlug: string;
  authorUsername: string | null;
  tagSlugs: string[];
}): string[] {
  const base = args.siteUrl.replace(/\/$/, "");
  const urls = [
    `${base}/p/${args.postSlug}`,
    `${base}/`,
    ...args.tagSlugs.map((s) => `${base}/t/${s}`),
  ];
  if (args.authorUsername) urls.push(`${base}/u/${args.authorUsername}`);
  return urls;
}
```

- [ ] **Step 5.4: (GREEN) Прогнать — 5/5 PASS**

```bash
pnpm test tests/lib/indexnow.test.ts
```

- [ ] **Step 5.5: Коммит**

```bash
git add src/lib/indexnow.ts tests/lib/indexnow.test.ts
git commit -m "feat(plan-6): IndexNow ping lib (fire-and-forget, 5s timeout)"
```

---

## Task 6: Подключить `pingIndexNow` в server actions

**Files:**
- Modify: `src/server/posts.ts` (publishPost, republishPost, softDeletePost)
- Modify: `src/server/actions/moderation.ts` (adminHidePost, adminDeletePost, adminBanUser)

Spec §4.3 таблица «Где зовём». Без `await` — fire-and-forget. Каждый вызов завёрнут в `void` чтобы линтер не ругался.

- [ ] **Step 6.1: Прочесть `src/server/posts.ts` целиком**

Открой файл. Найди три экспорта: `publishPost(...)`, `republishPost(postId)`, `softDeletePost(postId)`. Тебе нужен доступ к: `post.slug`, `users.username` автора, `tags.slug` массиве тегов поста, и `getEnv().NEXTAUTH_URL`.

- [ ] **Step 6.2: Добавить хук в `publishPost`**

В конец функции `publishPost` (после `revalidatePath` или `redirect`, **перед** возвратом / в `try`-блоке — точнее **после успешного UPDATE**) добавить:

```ts
// IndexNow: пингуем после успешной публикации.
// Сейчас, не до revalidatePath/redirect — если pingIndexNow по какой-то причине бросит
// (он не должен — он ловит ошибки внутри), это не сломает основной поток.
const slug = ... /* post.slug — у тебя уже есть из UPDATE; если нет — добавь SELECT */;
const authorUsername = ... /* users.username — если нет в текущей выборке, добавь join */;
const tagSlugs = ... /* массив тегов через select из postTags+tags */;
void pingIndexNow(postUrlsForIndexNow({
  siteUrl: getEnv().NEXTAUTH_URL,
  postSlug: slug,
  authorUsername,
  tagSlugs,
}));
```

Импорты в начало файла:

```ts
import { pingIndexNow, postUrlsForIndexNow } from "@/lib/indexnow";
import { getEnv } from "@/lib/env";
```

**Важно:** конкретный JOIN/SELECT зависит от текущей формы `publishPost`. Если она уже делает `select` по slug — переиспользуй. Если нет — добавь маленький запрос:

```ts
const [meta] = await db
  .select({ slug: posts.slug, authorUsername: users.username })
  .from(posts)
  .innerJoin(users, eq(users.id, posts.authorId))
  .where(eq(posts.id, postId))
  .limit(1);

const tagSlugsList = await db
  .select({ slug: tags.slug })
  .from(postTags)
  .innerJoin(tags, eq(tags.id, postTags.tagId))
  .where(eq(postTags.postId, postId));

if (meta) {
  void pingIndexNow(postUrlsForIndexNow({
    siteUrl: getEnv().NEXTAUTH_URL,
    postSlug: meta.slug,
    authorUsername: meta.authorUsername,
    tagSlugs: tagSlugsList.map((t) => t.slug),
  }));
}
```

- [ ] **Step 6.3: `republishPost` — тот же хук**

Идентичный блок в конец `republishPost` (после `UPDATE`).

- [ ] **Step 6.4: `softDeletePost` — короткий пинг (только пост URL)**

```ts
const [meta] = await db
  .select({ slug: posts.slug })
  .from(posts).where(eq(posts.id, postId)).limit(1);
if (meta) {
  const base = getEnv().NEXTAUTH_URL.replace(/\/$/, "");
  void pingIndexNow([`${base}/p/${meta.slug}`]);
}
```

(Без тегов и автора — при soft-delete нам важно только что страница ушла; пинг служит сигналом «переиндексируйте, чтобы убрать»).

- [ ] **Step 6.5: `src/server/actions/moderation.ts` — `adminHidePost` и `adminDeletePost`**

Открой файл. В обе функции после успешного UPDATE — тот же блок что в Step 6.4 (slug → один URL → ping).

- [ ] **Step 6.6: `adminBanUser` — если у юзера есть посты, пинг профиля**

В конец `adminBanUser`:

```ts
const [u] = await db
  .select({ username: users.username })
  .from(users).where(eq(users.id, userId)).limit(1);
if (u?.username) {
  const base = getEnv().NEXTAUTH_URL.replace(/\/$/, "");
  void pingIndexNow([`${base}/u/${u.username}`]);
}
```

- [ ] **Step 6.7: tsc + smoke**

```bash
pnpm tsc --noEmit
pnpm test
```

Ожидание: tsc чисто, все тесты зелёные (`pingIndexNow` в `NODE_ENV=test` — no-op, fetch не вызывается; mock'ать в тестах action'ов не требуется).

Если какой-то тест на posts/moderation падает — посмотри, не из-за того ли что новый код кидает на пустой `meta`. Проверь, что все `select`-ы оборачиваются в `if (meta)`.

- [ ] **Step 6.8: Коммит**

```bash
git add src/server/posts.ts src/server/actions/moderation.ts
git commit -m "feat(plan-6): pingIndexNow on publish/update/hide/delete/ban actions"
```

---

## Task 7: `JsonLd.tsx` микро-компонент

**Files:**
- Create: `src/components/seo/JsonLd.tsx`

Это маленькая обёртка для DRY. Без TDD — компонент тривиальный.

- [ ] **Step 7.1: Создать файл**

```tsx
// src/components/seo/JsonLd.tsx
// Сервер-компонент. Встраивает JSON-LD объект в DOM как <script type="application/ld+json">.
// `JSON.stringify` сам экранирует кавычки внутри строк; внешний </script> в значениях
// невозможен из-за того что мы стрейпим только plain objects из jsonld.ts.

import type { JsonLd as LdObject } from "@/lib/jsonld";

export function JsonLd({ data }: { data: LdObject }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}
```

- [ ] **Step 7.2: Smoke**

```bash
pnpm tsc --noEmit
```

Ожидание: чисто.

- [ ] **Step 7.3: Коммит**

```bash
git add src/components/seo/JsonLd.tsx
git commit -m "feat(plan-6): <JsonLd> wrapper component for schema.org snippets"
```

---

## Task 8: Подключить JSON-LD на публичные страницы

**Files:**
- Modify: `src/app/(public)/(feed)/page.tsx` (home) — `WebSite` JSON-LD + `generateMetadata`
- Modify: `src/app/(public)/p/[slug]/page.tsx` — `BlogPosting` + `BreadcrumbList` + OG image выбор в `generateMetadata`
- Modify: `src/app/(public)/(feed)/t/[slug]/page.tsx` — `BreadcrumbList`
- Modify: `src/app/(public)/(feed)/u/[username]/page.tsx` — `BreadcrumbList`

Spec §4.1 таблица «Где какой JSON-LD».

- [ ] **Step 8.1: Home `(feed)/page.tsx`** — `WebSite` JSON-LD

В начало файла (или туда где находится JSX верхнего уровня) добавь импорт:

```ts
import { JsonLd } from "@/components/seo/JsonLd";
import { buildWebSiteJsonLd } from "@/lib/jsonld";
import { getEnv } from "@/lib/env";
```

В JSX (после открытия `return`-блока, перед основным контентом) добавь:

```tsx
<JsonLd data={buildWebSiteJsonLd(getEnv().NEXTAUTH_URL)} />
```

Если у страницы ещё нет `generateMetadata` или `metadata` — добавь `metadata` объект сверху файла:

```ts
import type { Metadata } from "next";
export const metadata: Metadata = {
  openGraph: { type: "website" },
};
```

- [ ] **Step 8.2: Пост-страница `/p/[slug]/page.tsx`**

Открой файл. Найди `generateMetadata` (он уже есть и формирует `openGraph`). Расширь:

```ts
export async function generateMetadata({ params }: { params: Promise<Params> }) {
  const { slug } = await params;
  const post = await loadPost(slug);
  if (!post) return {};
  if (post.deletedAt) return {};
  if (post.hiddenByAdminAt) return {};
  if (post.status === "draft") return {};

  const siteUrl = getEnv().NEXTAUTH_URL.replace(/\/$/, "");
  const ogImage = post.coverUrl ?? `${siteUrl}/og/${post.slug}`;
  const url = `${siteUrl}/p/${post.slug}`;
  const description = post.contentHtml
    ? post.contentHtml.replace(/<[^>]*>/g, "").slice(0, 160)
    : post.title;

  return {
    title: post.title,
    description,
    alternates: { canonical: url },
    openGraph: {
      type: "article",
      url,
      title: post.title,
      description,
      images: [{ url: ogImage, width: 1200, height: 630 }],
      publishedTime: post.pubAt?.toISOString(),
    },
    twitter: {
      card: "summary_large_image",
      title: post.title,
      description,
      images: [ogImage],
    },
  };
}
```

В JSX `PostPage` (на верхнем уровне внутри `<article>`) добавь:

```tsx
<JsonLd
  data={buildBlogPostingJsonLd({
    post: {
      slug: post.slug,
      title: post.title,
      excerpt: (post.contentHtml ?? "").replace(/<[^>]*>/g, "").slice(0, 200),
      pubAt: post.pubAt,
      updatedAt: post.pubAt, // updatedAt поста читается отдельно если нужно — пока используем pubAt
      coverUrl: post.coverUrl,
      contentHtml: post.contentHtml ?? "",
    },
    author: { username: post.authorUsername, name: null },
    tags: postTagsList.map((t) => ({ name: t.name })),
    siteUrl: getEnv().NEXTAUTH_URL,
  })}
/>
<JsonLd
  data={buildBreadcrumbJsonLd([
    { name: "Главная", url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + "/" },
    { name: post.title, url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + `/p/${post.slug}` },
  ])}
/>
```

Импорты:

```ts
import { JsonLd } from "@/components/seo/JsonLd";
import { buildBlogPostingJsonLd, buildBreadcrumbJsonLd } from "@/lib/jsonld";
import { getEnv } from "@/lib/env";
```

Замечание: если в `loadPost` нет `updatedAt` поста — добавь его в `select` (`updatedAt: posts.updatedAt`) и используй в JSON-LD вместо `pubAt`.

- [ ] **Step 8.3: Тег-страница `(feed)/t/[slug]/page.tsx`** — `BreadcrumbList`

Спросишь страничный тайтл/тег, вставь после `<Header>` или в начало контента:

```tsx
<JsonLd
  data={buildBreadcrumbJsonLd([
    { name: "Главная", url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + "/" },
    { name: "Темы", url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + "/tags" },
    { name: tag.name, url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + `/t/${tag.slug}` },
  ])}
/>
```

Подставь имя/слаг тега из текущей загрузки в page-компоненте.

- [ ] **Step 8.4: Юзер-страница `(feed)/u/[username]/page.tsx`** — `BreadcrumbList`

```tsx
<JsonLd
  data={buildBreadcrumbJsonLd([
    { name: "Главная", url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + "/" },
    { name: `@${username}`, url: getEnv().NEXTAUTH_URL.replace(/\/$/, "") + `/u/${username}` },
  ])}
/>
```

- [ ] **Step 8.5: `robots: noindex` на приватные страницы**

В `src/app/(app)/new/page.tsx`, `src/app/(app)/edit/[id]/page.tsx`, `src/app/(app)/drafts/page.tsx` (если есть), `src/app/banned/page.tsx` — добавь `metadata`:

```ts
export const metadata = {
  robots: { index: false, follow: false },
};
```

Если в файле уже есть `metadata` — расширь его полем `robots`.

- [ ] **Step 8.6: Smoke**

```bash
pnpm tsc --noEmit
pnpm test
```

- [ ] **Step 8.7: Манульная проверка — открыть страницу и увидеть JSON-LD**

Запусти dev:
```bash
pnpm dev
```

В браузере открой `/p/<любой опубликованный slug>` (если есть). View Page Source → найди `<script type="application/ld+json">`. Внутри должен быть валидный JSON c `@type: BlogPosting`.

Если страниц нет — забей на manual, тесты будут чек'ить структуру.

- [ ] **Step 8.8: Коммит**

```bash
git add src/app/\(public\)/p/\[slug\]/page.tsx \
        "src/app/(public)/(feed)/page.tsx" \
        "src/app/(public)/(feed)/t/[slug]/page.tsx" \
        "src/app/(public)/(feed)/u/[username]/page.tsx" \
        src/app/\(app\)/new/page.tsx \
        src/app/\(app\)/edit/\[id\]/page.tsx \
        src/app/banned/page.tsx
git commit -m "feat(plan-6): JSON-LD + Metadata API on public pages, noindex on private"
```

Команда `git add` с escaped paths может варьироваться в зависимости от shell — если что, делай `git add -A` (после `git status` чтобы убедиться что ничего лишнего нет).

---

## Task 9: `src/app/robots.ts` **(TDD)**

**Files:**
- Create: `src/app/robots.ts`
- Create: `tests/seo/robots.test.ts`

Spec §3.1 + §4.1 robots block.

- [ ] **Step 9.1: (TDD RED) Тесты**

```ts
// tests/seo/robots.test.ts
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import robots from "@/app/robots";
import { _resetEnvCacheForTests } from "@/lib/env";

const ORIG = process.env.NEXTAUTH_URL;

describe("robots()", () => {
  beforeEach(() => {
    process.env.NEXTAUTH_URL = "https://example.ru";
    _resetEnvCacheForTests();
  });
  afterEach(() => {
    process.env.NEXTAUTH_URL = ORIG;
    _resetEnvCacheForTests();
  });

  it("disallow содержит все приватные пути", () => {
    const result = robots();
    const rule = Array.isArray(result.rules) ? result.rules[0] : result.rules;
    expect(rule.userAgent).toBe("*");
    expect(rule.allow).toBe("/");
    const disallow = Array.isArray(rule.disallow) ? rule.disallow : [rule.disallow];
    expect(disallow).toEqual(expect.arrayContaining([
      "/drafts", "/edit/", "/new", "/admin", "/banned", "/auth/", "/api/", "/dev/",
    ]));
  });

  it("sitemap указывает на NEXTAUTH_URL/sitemap.xml", () => {
    const result = robots();
    expect(result.sitemap).toBe("https://example.ru/sitemap.xml");
  });
});
```

- [ ] **Step 9.2: (RED) Прогнать — fail (модуля нет)**

```bash
pnpm test tests/seo/robots.test.ts
```

- [ ] **Step 9.3: Реализовать `src/app/robots.ts`**

```ts
// src/app/robots.ts
import type { MetadataRoute } from "next";
import { getEnv } from "@/lib/env";

export default function robots(): MetadataRoute.Robots {
  const siteUrl = getEnv().NEXTAUTH_URL.replace(/\/$/, "");
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",
        disallow: ["/drafts", "/edit/", "/new", "/admin", "/banned", "/auth/", "/api/", "/dev/"],
      },
    ],
    sitemap: `${siteUrl}/sitemap.xml`,
  };
}
```

- [ ] **Step 9.4: (GREEN) Прогнать — 2/2 PASS**

```bash
pnpm test tests/seo/robots.test.ts
```

- [ ] **Step 9.5: Manual smoke**

```bash
pnpm dev
curl -s http://localhost:3000/robots.txt
```

Ожидание: текст с `User-Agent: *`, `Allow: /`, серия `Disallow: /...`, `Sitemap: http://localhost:3000/sitemap.xml`.

- [ ] **Step 9.6: Коммит**

```bash
git add src/app/robots.ts tests/seo/robots.test.ts
git commit -m "feat(plan-6): robots.txt with disallow list + sitemap pointer"
```

---

## Task 10: Sitemap — синхронизация с `getEnv()` и тесты **(TDD)**

**Files:**
- Modify: `src/app/sitemap.ts` (минимально — переключить на `getEnv().NEXTAUTH_URL` если хочется единообразия, иначе оставить `siteConfig`; спека не требует обязательной замены)
- Create: `tests/seo/sitemap.test.ts`

Sitemap уже существует и работает (plan-5b). Тесты добавляем сейчас — проверяем визибилити-фильтры.

- [ ] **Step 10.1: (TDD RED) Тесты**

```ts
// tests/seo/sitemap.test.ts
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import sitemap from "@/app/sitemap";
import { getDb } from "@/lib/db";
import { posts, users, tags, postTags } from "@db/schema";
import { eq } from "drizzle-orm";
import { newId } from "@/lib/auth/id";

// Префикс уникальный per-test-run для cleanup
const RUN = `sm-${newId().slice(-8)}`;

const ids = {
  author: `${RUN}-author`,
  bannedAuthor: `${RUN}-banned`,
  postPublished: `${RUN}-pub`,
  postDraft: `${RUN}-draft`,
  postDeleted: `${RUN}-deleted`,
  postHidden: `${RUN}-hidden`,
  postOfBanned: `${RUN}-pubBan`,
  tagA: `${RUN}-tag-a`,
};

describe("sitemap()", () => {
  beforeAll(async () => {
    const db = getDb();
    await db.insert(users).values([
      { id: ids.author, email: `a-${RUN}@x.io`, username: `u_${RUN}_a` },
      { id: ids.bannedAuthor, email: `b-${RUN}@x.io`, username: `u_${RUN}_b`, bannedAt: new Date(), banReason: "test" },
    ]);
    await db.insert(tags).values([{ id: ids.tagA, slug: `tag-${RUN}-a`, name: "TagA" }]);
    await db.insert(posts).values([
      { id: ids.postPublished, authorId: ids.author, slug: `p-${RUN}-pub`, title: "Pub", content: { blocks: [] }, status: "published", pubAt: new Date() },
      { id: ids.postDraft, authorId: ids.author, slug: `p-${RUN}-draft`, title: "Draft", content: { blocks: [] }, status: "draft" },
      { id: ids.postDeleted, authorId: ids.author, slug: `p-${RUN}-del`, title: "Del", content: { blocks: [] }, status: "published", pubAt: new Date(), deletedAt: new Date() },
      { id: ids.postHidden, authorId: ids.author, slug: `p-${RUN}-hid`, title: "Hid", content: { blocks: [] }, status: "published", pubAt: new Date(), hiddenByAdminAt: new Date(), hiddenByAdminId: ids.author },
      { id: ids.postOfBanned, authorId: ids.bannedAuthor, slug: `p-${RUN}-ban`, title: "Ban", content: { blocks: [] }, status: "published", pubAt: new Date() },
    ]);
    await db.insert(postTags).values([{ postId: ids.postPublished, tagId: ids.tagA }]);
  });

  afterAll(async () => {
    const db = getDb();
    await db.delete(postTags).where(eq(postTags.tagId, ids.tagA));
    for (const p of [ids.postPublished, ids.postDraft, ids.postDeleted, ids.postHidden, ids.postOfBanned]) {
      await db.delete(posts).where(eq(posts.id, p));
    }
    await db.delete(tags).where(eq(tags.id, ids.tagA));
    for (const u of [ids.author, ids.bannedAuthor]) {
      await db.delete(users).where(eq(users.id, u));
    }
  });

  it("включает только published + не-deleted + не-hidden", async () => {
    const rows = await sitemap();
    const urls = rows.map((r) => r.url);
    expect(urls.some((u) => u.endsWith(`/p/p-${RUN}-pub`))).toBe(true);
    expect(urls.some((u) => u.endsWith(`/p/p-${RUN}-draft`))).toBe(false);
    expect(urls.some((u) => u.endsWith(`/p/p-${RUN}-del`))).toBe(false);
    expect(urls.some((u) => u.endsWith(`/p/p-${RUN}-hid`))).toBe(false);
  });

  it("исключает посты забаненных авторов на уровне /u/, но не /p/", async () => {
    const rows = await sitemap();
    const urls = rows.map((r) => r.url);
    // /p/p-RUN-ban — это пост забаненного, но сам он published+visible.
    // Поведение sitemap'а текущей реализации: фильтрует ТОЛЬКО /u/* по bannedAt,
    // /p/* — нет (мы НЕ скрываем посты, только профиль). Тест фиксирует это поведение.
    expect(urls.some((u) => u.endsWith(`/p/p-${RUN}-ban`))).toBe(true);
    expect(urls.some((u) => u.endsWith(`/u/u_${RUN}_b`))).toBe(false);
  });

  it("/u/* содержит только не-banned юзеров с published-постами", async () => {
    const rows = await sitemap();
    const urls = rows.map((r) => r.url);
    expect(urls.some((u) => u.endsWith(`/u/u_${RUN}_a`))).toBe(true);
  });

  it("содержит /, /tags и /t/<наш-тег>", async () => {
    const rows = await sitemap();
    const urls = rows.map((r) => r.url);
    expect(urls.some((u) => u.endsWith("/"))).toBe(true);
    expect(urls.some((u) => u.endsWith("/tags"))).toBe(true);
    expect(urls.some((u) => u.endsWith(`/t/tag-${RUN}-a`))).toBe(true);
  });
});
```

- [ ] **Step 10.2: (RED) Прогнать — должны падать если sitemap не работает на test-DB**

```bash
DATABASE_URL="postgres://app:<your-dev-password>@localhost:5432/app" pnpm test tests/seo/sitemap.test.ts
```

Если sitemap уже корректно фильтрует — все 4 PASS. Это «безопасный TDD»: фиксируем существующее поведение чтобы предотвратить регрессию.

- [ ] **Step 10.3: Поведение sitemap'а уже соответствует — никакой правки**

Существующий `src/app/sitemap.ts` уже фильтрует `published + !deleted + !hidden` и `/u/*` для не-banned юзеров с published-постами. Не трогаем.

(Если хочется унифицировать siteUrl — заменить `siteConfig.url` на `getEnv().NEXTAUTH_URL.replace(/\/$/, "")`. Не обязательно — sitemap прошёл DoD plan-5b.)

- [ ] **Step 10.4: Manual smoke**

```bash
curl -s http://localhost:3000/sitemap.xml | head -50
```

Ожидание: XML с `<urlset>`, серия `<url>`-блоков, у `/p/*` есть `<lastmod>`.

- [ ] **Step 10.5: Коммит**

```bash
git add tests/seo/sitemap.test.ts
git commit -m "test(plan-6): sitemap visibility filters (published, !deleted, !hidden, !banned)"
```

---

## Task 11: OG-картинки — `src/app/og/[slug]/route.tsx`

**Files:**
- Create: `src/app/og/[slug]/route.tsx`

Spec §4.2. Тестируем минимально (вручную через `curl`); spec §7.2 + §12 явно говорят: vitest+jsdom плохо запускает `next/og`, оставляем manual.

- [ ] **Step 11.1: Создать файл**

```tsx
// src/app/og/[slug]/route.tsx
import { ImageResponse } from "next/og";
import { and, eq, isNull } from "drizzle-orm";
import { getDb } from "@/lib/db";
import { posts, users } from "@db/schema";

export const runtime = "nodejs";
// next/og сам кеширует output; revalidate=false означает «не обновлять
// автоматически, пока поста не обновили» — для нас ок.
export const revalidate = false;

export async function GET(
  _req: Request,
  { params }: { params: Promise<{ slug: string }> },
) {
  const { slug } = await params;

  const [row] = await getDb()
    .select({
      title: posts.title,
      pubAt: posts.pubAt,
      username: users.username,
      name: users.name,
    })
    .from(posts)
    .innerJoin(users, eq(users.id, posts.authorId))
    .where(and(
      eq(posts.slug, slug),
      eq(posts.status, "published"),
      isNull(posts.deletedAt),
      isNull(posts.hiddenByAdminAt),
    ))
    .limit(1);

  if (!row) return new Response("Not found", { status: 404 });

  const dateLabel = row.pubAt
    ? new Intl.DateTimeFormat("ru-RU", { day: "numeric", month: "long" }).format(row.pubAt)
    : "";
  const authorLabel = row.name ?? row.username ?? "Аноним";

  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          background: "linear-gradient(135deg, #0f172a 0%, #1e293b 100%)",
          display: "flex",
          flexDirection: "column",
          padding: "64px",
          fontFamily: "system-ui",
          color: "white",
        }}
      >
        <div style={{ fontSize: 32, fontWeight: 700, opacity: 0.7 }}>Skelet</div>
        <div style={{ flex: 1, display: "flex", alignItems: "center" }}>
          <div
            style={{
              fontSize: 64,
              fontWeight: 800,
              lineHeight: 1.15,
              maxHeight: 64 * 1.15 * 3,
              overflow: "hidden",
              display: "-webkit-box",
              WebkitLineClamp: 3,
              WebkitBoxOrient: "vertical",
            }}
          >
            {row.title}
          </div>
        </div>
        <div
          style={{
            fontSize: 28,
            opacity: 0.7,
            borderTop: "1px solid rgba(255,255,255,0.2)",
            paddingTop: 24,
          }}
        >
          @{authorLabel} · {dateLabel}
        </div>
      </div>
    ),
    { width: 1200, height: 630 },
  );
}
```

- [ ] **Step 11.2: Manual smoke на dev**

```bash
pnpm dev
```

В браузере (или `curl -I`):
```bash
curl -I http://localhost:3000/og/<существующий-slug>
```
Ожидание: `200 OK`, `Content-Type: image/png` (или `image/jpeg`/`image/webp` в зависимости от того что отдаёт next/og — главное чтобы был image).

Без существующего слага:
```bash
curl -I http://localhost:3000/og/this-slug-does-not-exist
```
Ожидание: `404`.

Если падает с ошибкой `ImageResponse not found` или ругается на runtime — посмотри в spec §11 risk #5: возможно, придётся включить `experimental.imageOptimization` или подобрать другой подход. Сообщи об этом до перехода к Task 12.

- [ ] **Step 11.3: tsc**

```bash
pnpm tsc --noEmit
```

- [ ] **Step 11.4: Коммит**

```bash
git add src/app/og/\[slug\]/route.tsx
git commit -m "feat(plan-6): dynamic OG images via next/og ImageResponse"
```

---

## Task 12: `<YandexMetrika>` component

**Files:**
- Create: `src/components/analytics/YandexMetrika.tsx`
- Create: `tests/components/analytics/YandexMetrika.test.tsx`
- Modify: `src/app/layout.tsx`

Spec §4.4. `<Script strategy="afterInteractive">` + `<noscript>`-pixel. Без webvisor.

- [ ] **Step 12.1: Создать компонент**

```tsx
// src/components/analytics/YandexMetrika.tsx
"use client";

import Script from "next/script";

interface Props {
  counterId: string;
}

export function YandexMetrika({ counterId }: Props) {
  const id = counterId;
  return (
    <>
      <Script
        id="ym-init"
        strategy="afterInteractive"
        dangerouslySetInnerHTML={{
          __html: `
            (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
            m[i].l=1*new Date();
            for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
            k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
            (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");
            ym(${JSON.stringify(id)}, "init", { clickmap: true, trackLinks: true, accurateTrackBounce: true });
          `,
        }}
      />
      <noscript>
        <div>
          <img
            src={`https://mc.yandex.ru/watch/${encodeURIComponent(id)}`}
            style={{ position: "absolute", left: "-9999px" }}
            alt=""
          />
        </div>
      </noscript>
    </>
  );
}
```

- [ ] **Step 12.2: Подключить в `src/app/layout.tsx`**

Добавь импорт:

```ts
import { YandexMetrika } from "@/components/analytics/YandexMetrika";
```

В `<body>` (перед `<ThemeProvider>` или сразу после, не важно — это `<Script>`, который сам себя insert'нет в DOM):

```tsx
{process.env.NODE_ENV === "production" && process.env.YANDEX_METRIKA_ID && (
  <YandexMetrika counterId={process.env.YANDEX_METRIKA_ID} />
)}
```

- [ ] **Step 12.3: (TDD RED) Тест**

```tsx
// tests/components/analytics/YandexMetrika.test.tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import { YandexMetrika } from "@/components/analytics/YandexMetrika";

describe("<YandexMetrika>", () => {
  it("содержит counterId в init-snippet и noscript-pixel", () => {
    const { container } = render(<YandexMetrika counterId="12345678" />);
    const script = container.querySelector("#ym-init");
    expect(script?.innerHTML).toContain("12345678");
    const img = container.querySelector("noscript img") ?? container.querySelector("img");
    // jsdom не рендерит noscript-внутренности в DOM; img может быть null —
    // это OK, основной тест — что counterId попадает в init-snippet
    if (img) expect(img.getAttribute("src")).toContain("12345678");
  });
});
```

- [ ] **Step 12.4: Прогнать — 1/1 PASS**

```bash
pnpm test tests/components/analytics/YandexMetrika.test.tsx
```

Замечание: `next/script` в jsdom может выдать warning о `dangerouslySetInnerHTML` — это OK, тест проверяет содержимое атрибута, а не factual loading.

- [ ] **Step 12.5: Smoke — production build не падает**

```bash
NODE_ENV=production pnpm build
```

Ожидание: build зелёный. (С пустым `YANDEX_METRIKA_ID` Метрика не подгружается, но build на её отсутствие не падает.)

- [ ] **Step 12.6: Коммит**

```bash
git add src/components/analytics/YandexMetrika.tsx tests/components/analytics/YandexMetrika.test.tsx src/app/layout.tsx
git commit -m "feat(plan-6): Yandex.Metrika component (production-only, no webvisor)"
```

---

## Task 13: `/privacy` страница

**Files:**
- Create: `src/app/(public)/privacy/page.tsx`
- Create: `tests/app/privacy.test.tsx`

Spec §3.4 + §4.4. Серверный компонент, читает строки из `theme/content.ts.privacy`.

- [ ] **Step 13.1: Создать страницу**

```tsx
// src/app/(public)/privacy/page.tsx
import type { Metadata } from "next";
import { content } from "@theme/content";

export const metadata: Metadata = {
  title: "Политика конфиденциальности",
  // indexable — пусть поисковики видят политику
};

export default function PrivacyPage() {
  const p = content.privacy;
  const email = content.site.contactEmail;
  return (
    <main className="container mx-auto max-w-2xl py-12 px-4 prose prose-neutral dark:prose-invert">
      <h1>{p.title}</h1>
      <p>{p.intro}</p>

      <h2>{p.section.whoWeAre}</h2>
      <p>{p.section.whoWeAreBody}</p>

      <h2>{p.section.whatWeCollect}</h2>
      <p>{p.section.whatWeCollectBody}</p>

      <h2>{p.section.cookies}</h2>
      <p>{p.section.cookiesBody}</p>

      <h2>{p.section.delete}</h2>
      <p>{p.section.deleteBody}</p>

      <h2>{p.section.contact}</h2>
      <p>
        {p.contact.split(email)[0]}
        <a href={`mailto:${email}`}>{email}</a>
      </p>

      <p className="text-sm text-muted-foreground">{p.updatedAt}</p>
    </main>
  );
}
```

- [ ] **Step 13.2: (TDD RED) Тест**

```tsx
// tests/app/privacy.test.tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import PrivacyPage from "@/app/(public)/privacy/page";
import { content } from "@theme/content";

describe("/privacy", () => {
  it("содержит контактный email и упоминание Метрики", () => {
    const { container, getByText } = render(<PrivacyPage />);
    expect(container.textContent).toContain(content.site.contactEmail);
    expect(container.textContent?.toLowerCase()).toMatch(/метрик/);
    expect(getByText(content.privacy.title)).toBeTruthy();
  });
});
```

- [ ] **Step 13.3: GREEN**

```bash
pnpm test tests/app/privacy.test.tsx
```

Ожидание: 1/1 PASS.

- [ ] **Step 13.4: Manual smoke**

```bash
pnpm dev
```

Открой `http://localhost:3000/privacy` — увидь страницу с заголовком, секциями, email-ссылкой.

- [ ] **Step 13.5: Коммит**

```bash
git add "src/app/(public)/privacy/page.tsx" tests/app/privacy.test.tsx
git commit -m "feat(plan-6): /privacy page with content from theme"
```

---

## Task 14: Футер — disclaimer + ссылка на /privacy

**Files:**
- Modify: `src/components/layout/Footer.tsx`
- Create: `tests/components/layout/Footer.test.tsx`

Spec §4.4.

- [ ] **Step 14.1: Обновить `Footer.tsx`**

```tsx
// src/components/layout/Footer.tsx
import Link from "next/link";
import { content } from "@theme/content";

export function Footer() {
  return (
    <footer className="border-t border-border mt-16">
      <div className="container mx-auto px-4 py-8 max-w-[1200px] flex flex-col md:flex-row md:items-center md:justify-between gap-4 text-sm text-muted-foreground">
        <div>{content.copyright}</div>
        <nav className="flex flex-wrap gap-x-6 gap-y-2">
          <a href="/about" className="hover:text-foreground">{content.footer.about}</a>
          <a href="/rules" className="hover:text-foreground">{content.footer.rules}</a>
          <a href="/contacts" className="hover:text-foreground">{content.footer.contacts}</a>
          <Link href="/privacy" className="hover:text-foreground">
            {content.footer.privacyLink}
          </Link>
        </nav>
      </div>
      <div className="container mx-auto px-4 pb-6 max-w-[1200px] text-xs text-muted-foreground">
        {content.footer.disclaimer}
      </div>
    </footer>
  );
}
```

- [ ] **Step 14.2: (TDD) Тест**

```tsx
// tests/components/layout/Footer.test.tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import { Footer } from "@/components/layout/Footer";
import { content } from "@theme/content";

describe("<Footer>", () => {
  it("содержит disclaimer и ссылку на /privacy", () => {
    const { container, getByText } = render(<Footer />);
    expect(container.textContent).toContain(content.footer.disclaimer);
    const link = getByText(content.footer.privacyLink) as HTMLAnchorElement;
    expect(link.getAttribute("href")).toBe("/privacy");
  });
});
```

- [ ] **Step 14.3: GREEN**

```bash
pnpm test tests/components/layout/Footer.test.tsx
```

- [ ] **Step 14.4: Smoke**

```bash
pnpm tsc --noEmit
```

- [ ] **Step 14.5: Коммит**

```bash
git add src/components/layout/Footer.tsx tests/components/layout/Footer.test.tsx
git commit -m "feat(plan-6): footer disclaimer + privacy link"
```

---

## Task 15: Caddyfile — security headers + www→apex redir + /og/* в static

**Files:**
- Modify: `Caddyfile`

Spec §4.5. Финальная версия:

- [ ] **Step 15.1: Заменить Caddyfile**

```caddy
{
    email {$LETSENCRYPT_EMAIL}
}

www.{$DOMAIN} {
    redir https://{$DOMAIN}{uri} permanent
}

{$DOMAIN} {
    encode gzip zstd
    reverse_proxy app:3000

    @static {
        path /_next/static/*
        path /favicon.ico
        path /icons/*
        path /og/*
    }
    header @static Cache-Control "public, max-age=31536000, immutable"

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
        Permissions-Policy "interest-cohort=()"
        X-Frame-Options DENY
        -Server
    }
}
```

- [ ] **Step 15.2: Валидация Caddyfile**

Если установлен Caddy локально:
```bash
caddy validate --config Caddyfile --adapter caddyfile
```

Если Caddy локально не установлен — валидация через Docker (требует `DOMAIN` в env):
```bash
DOMAIN=localhost LETSENCRYPT_EMAIL=test@test.ru docker run --rm -v "$PWD/Caddyfile:/etc/caddy/Caddyfile" caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile
```

Ожидание: `Valid configuration`.

Если ругается на `{$LETSENCRYPT_EMAIL}` плейсхолдер — это OK для validate (envsubst делается при старте контейнера); если хочется не зависеть от env при валидации, замени глобальный блок на `{ acme_dns; }` или временно убери `email` на момент проверки.

- [ ] **Step 15.3: Коммит**

```bash
git add Caddyfile
git commit -m "feat(plan-6): Caddyfile prod headers, www→apex redir, /og/* in static cache"
```

---

## Task 16: `docker-compose.yml` — healthcheck app + backup entrypoint + LETSENCRYPT_EMAIL

**Files:**
- Modify: `docker-compose.yml`

Spec §4.5.

- [ ] **Step 16.1: Применить точечный патч**

Открой `docker-compose.yml`. Поменяй три места:

**1) caddy.environment** — добавь `LETSENCRYPT_EMAIL`:

```yaml
caddy:
  image: caddy:2-alpine
  restart: unless-stopped
  ports: ["80:80", "443:443"]
  volumes:
    - ./Caddyfile:/etc/caddy/Caddyfile:ro
    - caddy_data:/data
    - caddy_config:/config
  environment:
    - DOMAIN=${DOMAIN}
    - LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}
  depends_on: [app]
```

**2) app** — добавить healthcheck:

```yaml
app:
  build: .
  restart: unless-stopped
  env_file: .env
  environment:
    DATABASE_URL: postgres://app:${DB_PASSWORD}@db:5432/app
  depends_on:
    db:
      condition: service_healthy
  healthcheck:
    test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 30s
```

Замечание: `wget` есть в alpine-base, который Next.js standalone Dockerfile использует. Если Dockerfile построен на debian-slim — заменить `wget --spider -q` на `curl -fsS`. Проверь `Dockerfile`:
```bash
grep -E "FROM (node|debian)" Dockerfile
```

**3) backup** — новый entrypoint + дополнительный volume:

```yaml
backup:
  image: postgres:16-alpine
  restart: unless-stopped
  env_file: .env
  volumes:
    - ./scripts/backup.sh:/backup.sh:ro
    - ./scripts/backup-entrypoint.sh:/backup-entrypoint.sh:ro
  entrypoint: ["sh", "/backup-entrypoint.sh"]
  depends_on: [db]
```

- [ ] **Step 16.2: Валидация compose**

```bash
docker compose config > /dev/null && echo "OK"
```

Ожидание: `OK`. Если упадёт по env (нет `DOMAIN`, `LETSENCRYPT_EMAIL`, `DB_PASSWORD`) — временно выставь их через `DOMAIN=localhost LETSENCRYPT_EMAIL=test@test.ru DB_PASSWORD=x docker compose config`.

- [ ] **Step 16.3: Коммит**

```bash
git add docker-compose.yml
git commit -m "feat(plan-6): docker-compose app healthcheck + backup entrypoint + LETSENCRYPT_EMAIL"
```

---

## Task 17: `scripts/backup-entrypoint.sh` + финальный `scripts/backup.sh`

**Files:**
- Replace: `scripts/backup.sh` (placeholder → реальный)
- Create: `scripts/backup-entrypoint.sh`

Spec §4.5.

- [ ] **Step 17.1: Заменить `scripts/backup.sh`**

```sh
#!/bin/sh
# Запускается планировщиком в backup-entrypoint.sh.
# Делает дамп Postgres, gzip'ит, заливает в S3 Cold, удаляет локальную копию.
set -e

DATE=$(date +%Y-%m-%d-%H%M)
FILE="backup-${DATE}.sql.gz"
LOCAL="/tmp/${FILE}"

echo "[backup] starting at $(date -Iseconds)"
PGPASSWORD="${DB_PASSWORD}" pg_dump \
  -h db -U app -d app \
  --no-owner --no-acl --format=plain \
  | gzip -9 > "${LOCAL}"
SIZE=$(du -h "${LOCAL}" | cut -f1)
echo "[backup] dump done: ${FILE} (${SIZE})"

aws --endpoint-url="${BACKUP_S3_ENDPOINT}" \
    s3 cp "${LOCAL}" "s3://${BACKUP_S3_BUCKET}/db/${FILE}"

rm "${LOCAL}"
echo "[backup] uploaded: s3://${BACKUP_S3_BUCKET}/db/${FILE}"
```

`chmod +x scripts/backup.sh` (если git не сохранит флаг — это нормально, в контейнере вызывается через `sh /backup.sh`).

- [ ] **Step 17.2: Создать `scripts/backup-entrypoint.sh`**

```sh
#!/bin/sh
# Entrypoint для backup-контейнера. Ставит aws-cli, ждёт до 03:00 MSK, вызывает backup.sh.
# Идём бесконечным циклом — restart=unless-stopped в compose продолжит при сбое VPS.
set -e

apk add --no-cache aws-cli tzdata
export TZ=Europe/Moscow
export AWS_ACCESS_KEY_ID="${BACKUP_S3_ACCESS_KEY_ID}"
export AWS_SECRET_ACCESS_KEY="${BACKUP_S3_SECRET_ACCESS_KEY}"
export AWS_DEFAULT_REGION=ru-central1

while true; do
  NEXT=$(date -d "today 03:00" +%s)
  NOW=$(date +%s)
  if [ "${NOW}" -ge "${NEXT}" ]; then
    NEXT=$(date -d "tomorrow 03:00" +%s)
  fi
  SLEEP=$((NEXT - NOW))
  echo "[backup] sleeping ${SLEEP}s until $(date -d "@${NEXT}" -Iseconds)"
  sleep "${SLEEP}"
  sh /backup.sh || echo "[backup] FAILED at $(date -Iseconds)"
done
```

`chmod +x scripts/backup-entrypoint.sh`.

- [ ] **Step 17.3: Локальная проверка корректности shell-синтаксиса**

```bash
sh -n scripts/backup.sh && echo "backup.sh OK"
sh -n scripts/backup-entrypoint.sh && echo "entrypoint OK"
```

Ожидание: оба `OK`.

- [ ] **Step 17.4: (опц.) Сухая проверка — `apk add aws-cli` в контейнере работает**

```bash
docker run --rm postgres:16-alpine sh -c "apk add --no-cache aws-cli && aws --version"
```

Ожидание: `aws-cli/2.x.x ...`. Если фейлится — Spec §11.6 risk: aws-cli может оказаться в community-repo. Откатной план: установить через pip:

```sh
apk add --no-cache python3 py3-pip
pip install --break-system-packages awscli
```

Этот fallback опционален; первая попытка — `apk add aws-cli`.

- [ ] **Step 17.5: Коммит**

```bash
git add scripts/backup.sh scripts/backup-entrypoint.sh
git commit -m "feat(plan-6): daily Postgres backup to S3 Cold (03:00 MSK, --no-owner, gzip -9)"
```

---

## Task 18: `docs/DEPLOY.md` — пошаговая инструкция от пустого VPS до работающего сайта

**Files:**
- Create: `docs/DEPLOY.md`

- [ ] **Step 18.1: Создать файл по шаблону ниже**

```markdown
# Skelet — Deployment Guide

> Целевая платформа: **Timeweb Cloud VPS** 1×5GHz / 1GB / 15GB. Один сервер,
> docker-compose (caddy + app + db + backup), Let's Encrypt автоматически.

## 0. Pre-deploy smoke (на локальной машине)

Перед первым деплоем и перед каждым релизом:

1. `pnpm test` — зелёно
2. `pnpm tsc --noEmit` — зелёно
3. `pnpm build` — production-сборка локально прошла
4. `docker compose build app` — Docker-сборка локально проходит
5. (Опционально) Создать `.env.prod-test` с реальными prod-кредами Timeweb S3 + локальной БД, запустить `docker compose --env-file .env.prod-test up -d` и проверить:
   - `/` открывается, видны посты
   - `/p/[slug]` → JSON-LD в DOM, OG-meta в head
   - Upload через `/new` идёт в Timeweb
   - `/sitemap.xml`, `/robots.txt`, `/api/health`, `/og/[slug]`, `/privacy` отвечают корректно
   - Метрика в DOM (если `YANDEX_METRIKA_ID` указан и `NODE_ENV=production`)

Только после зелёного — деплой.

## 1. Создаём VPS на Timeweb

1. timeweb.cloud → Cloud Servers → Create
2. Образ: Ubuntu 22.04 LTS / 24.04 LTS
3. Тариф: **1×5GHz / 1GB / 15GB / 200 Мбит** (825 ₽/мес)
4. Сеть: IPv4 (+ 180 ₽/мес) + IPv6, **без приватной сети**
5. SSH-ключ: загрузить свой публичный ключ заранее (Profile → SSH keys)
6. Нажать Create. Через ~60 секунд VPS готов; запиши публичный IPv4.

## 2. DNS

У регистратора домена создать:

- A-запись `example.ru` → `<IPv4>`
- A-запись `www.example.ru` → `<IPv4>`
- (опц.) AAAA-записи на IPv6

TTL — оставить дефолтным. DNS пропагируется 5–60 минут; проверка:
```bash
dig +short example.ru
```

## 3. Первый вход на VPS

```bash
ssh root@<IP>
```

### 3.1. Базовая настройка

```bash
# Обновить пакеты
apt update && apt upgrade -y

# Создать non-root юзера
adduser skelet
usermod -aG sudo skelet
mkdir -p /home/skelet/.ssh
cp /root/.ssh/authorized_keys /home/skelet/.ssh/
chown -R skelet:skelet /home/skelet/.ssh
chmod 700 /home/skelet/.ssh
chmod 600 /home/skelet/.ssh/authorized_keys

# Запретить root-вход и пароли (по желанию, после проверки что skelet логинится)
# Редактировать /etc/ssh/sshd_config:
#   PermitRootLogin no
#   PasswordAuthentication no
# systemctl reload ssh
```

### 3.2. Установить Docker + compose plugin

```bash
curl -fsSL https://get.docker.com | sh
usermod -aG docker skelet
# logout / login заново
```

Проверка:
```bash
docker version
docker compose version
```

### 3.3. Firewall (по желанию — UFW)

```bash
sudo apt install ufw
sudo ufw allow OpenSSH
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
```

## 4. Деплой кода

```bash
ssh skelet@<IP>
git clone https://github.com/<you>/skelet.git
cd skelet
```

Создать `.env` на VPS (НЕ коммитим в git):

```bash
nano .env
```

Заполнить по `.env.example`. Сгенерировать секреты заранее на локалке:

```bash
openssl rand -base64 32   # NEXTAUTH_SECRET
openssl rand -base64 24   # DB_PASSWORD
openssl rand -hex 16      # INDEXNOW_KEY
```

`DOMAIN=example.ru`, `LETSENCRYPT_EMAIL=<твой email>`, `NEXTAUTH_URL=https://example.ru`,
`STORAGE_ENDPOINT=https://s3.timeweb.cloud`, `STORAGE_BUCKET=skelet-images`, ...

## 5. Создать S3-buckets в Timeweb

### 5.1. `skelet-images` (Standard)

1. Timeweb → S3 Storage → Create bucket
2. Name: `skelet-images`, Type: Standard, Public read: yes
3. CORS — разрешить `PUT` от `https://example.ru`:
   ```json
   [{
     "AllowedOrigins": ["https://example.ru"],
     "AllowedMethods": ["PUT", "GET", "HEAD"],
     "AllowedHeaders": ["*"],
     "MaxAgeSeconds": 3600
   }]
   ```
4. Service users → Create → права на bucket → запиши access/secret в `.env`
   (`STORAGE_ACCESS_KEY_ID`, `STORAGE_SECRET_ACCESS_KEY`)
5. `STORAGE_PUBLIC_BASE=https://skelet-images.s3.timeweb.cloud`

### 5.2. `skelet-backups` (Cold)

1. Create bucket → name: `skelet-backups`, Type: **Cold**
2. Lifecycle policy: delete objects older than **30 days**
3. Service users → отдельный (минимальные права: только `s3:PutObject` на этот bucket)
4. Запиши access/secret в `.env` (`BACKUP_S3_ACCESS_KEY_ID`, `BACKUP_S3_SECRET_ACCESS_KEY`)

## 6. Создать OAuth-приложения (prod)

Для каждого провайдера (Yandex, VK) создать prod-приложение с redirect URI:
- `https://example.ru/api/auth/callback/yandex`
- `https://example.ru/api/auth/callback/vk`

Записать `CLIENT_ID` / `CLIENT_SECRET` в `.env`.

## 7. Запуск

```bash
docker compose up -d
docker compose ps
docker compose logs -f caddy   # увидеть процесс получения сертификата
```

Сертификат получается автоматически ~30–60 секунд после первого запроса. Если в логах
`acme: error...` — проверь:
- DNS уже указывает на VPS (`dig +short example.ru`)
- 80/443 открыты на firewall'е (`curl -I http://example.ru` снаружи)

Применить миграции БД:
```bash
docker compose exec app pnpm db:migrate
```

(или они применяются автоматически — зависит от Dockerfile entrypoint; см. `scripts/entrypoint.sh`).

Открыть `https://example.ru` — должен открыться сайт.

## 8. Post-deploy ручные шаги

### 8.1. IndexNow verification file

На VPS:
```bash
cd skelet
echo "$INDEXNOW_KEY" > "public/${INDEXNOW_KEY}.txt"
git add "public/${INDEXNOW_KEY}.txt"
git commit -m "chore: add IndexNow verification file"
# Перезапустить app, чтобы файл попал в next-сборку
docker compose up -d --build app
```

Проверить:
```bash
curl https://example.ru/${INDEXNOW_KEY}.txt
# Ожидание: значение того же ключа
```

### 8.2. Yandex.Metrika

1. metrica.yandex.ru → создать счётчик
2. Записать ID в `.env` (`YANDEX_METRIKA_ID=...`)
3. `docker compose up -d --build app`
4. В DevTools Network на любой странице должен загружаться `mc.yandex.ru/metrika/tag.js`

### 8.3. Yandex Webmaster

1. webmaster.yandex.ru → Add site → `example.ru`
2. Verify: метод HTML-file → дают файл `yandex_<hash>.html` → положить в `public/` → redeploy
3. Submit sitemap: `https://example.ru/sitemap.xml`

### 8.4. Google Search Console

1. search.google.com/search-console → Add property → URL prefix → `https://example.ru`
2. Verify: метод HTML-file → положить файл в `public/` → redeploy
3. Submit sitemap: `https://example.ru/sitemap.xml`

### 8.5. UptimeRobot + Telegram

1. uptimerobot.com → регистрация (free)
2. Telegram: `/newbot` у @BotFather → имя `skelet_status_bot` → токен
3. У `@userinfobot` `/start` → твой `chat_id`
4. UptimeRobot → My Settings → Add Alert Contact → **Webhook**
   - Friendly name: `Telegram`
   - URL: `https://api.telegram.org/bot<BOT_TOKEN>/sendMessage`
   - POST body type: `JSON`
   - POST value: `{"chat_id":"<CHAT_ID>","text":"*alertTypeFriendlyName* - *monitorFriendlyName* (*alertDetails*)"}`
5. Add Monitor → HTTP(s)
   - Friendly name: `skelet`
   - URL: `https://example.ru/api/health`
   - Monitoring interval: 5 min, Timeout: 30s
   - Alert when: 2 consecutive failures
   - Alert Contacts: только что созданный Webhook
6. Test alert: pause monitor → ждать 5 минут → должен прилететь в Telegram.

## 9. Регулярные операции

### Обновление кода

```bash
ssh skelet@<IP>
cd skelet
git pull
docker compose up -d --build app
# Если изменялась схема:
docker compose exec app pnpm db:migrate
```

### Просмотр логов

```bash
docker compose logs -f --tail=200 app
docker compose logs -f --tail=200 caddy
docker compose logs -f --tail=200 backup
```

### Проверка бэкапов

В Timeweb S3 Cold кабинете → `skelet-backups/db/` → должен появляться новый файл каждое утро ~03:00 MSK.

## 10. Что делать при OOM на сборке

`pnpm build` на 1GB VPS может упасть в OOM. Добавить swap:

```bash
sudo fallocate -l 3G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
free -h
```

После этого `docker compose up -d --build app` должно проходить. Если и со swap не идёт — апгрейд тарифа Timeweb до 2GB.

## 11. Troubleshooting

| Симптом | Возможная причина | Действие |
|---|---|---|
| `acme: 404` в Caddy | DNS не указывает на VPS | `dig +short example.ru` → должен быть IP VPS |
| Сайт открывается по HTTP, но не по HTTPS | 443 закрыт firewall'ом | `sudo ufw status` → разрешить 443 |
| Upload в admin падает 503 | `STORAGE_*` пустые / неправильные | `docker compose exec app printenv | grep STORAGE` |
| Бэкапы не появляются в S3 | Не настроен `BACKUP_S3_*` или нет прав на bucket | `docker compose logs backup` |
| OG-картинка не рендерится | `next/og` падает в standalone | Smoke `curl -I https://example.ru/og/<slug>` — если 500, см. plan-06 risk #5 |

## 12. Восстановление из бэкапа

См. отдельный документ: [`docs/RECOVERY.md`](./RECOVERY.md).
```

- [ ] **Step 18.2: Коммит**

```bash
git add docs/DEPLOY.md
git commit -m "docs(plan-6): DEPLOY.md — VPS-to-running-site step-by-step"
```

---

## Task 19: `docs/RECOVERY.md` — восстановление из бэкапа

**Files:**
- Create: `docs/RECOVERY.md`

- [ ] **Step 19.1: Создать файл**

```markdown
# Skelet — Disaster Recovery

> Восстановление БД из бэкапа в S3 Cold (`skelet-backups`).

## 1. Получить последний дамп

На VPS (или с локалки, если у тебя есть credentials):

```bash
# Установить aws-cli если ещё нет
sudo apt install awscli

# Указать credentials BACKUP_S3_*
export AWS_ACCESS_KEY_ID=...
export AWS_SECRET_ACCESS_KEY=...
export AWS_DEFAULT_REGION=ru-central1

# Список доступных дампов
aws --endpoint-url=https://s3.timeweb.cloud s3 ls s3://skelet-backups/db/

# Скачать нужный
aws --endpoint-url=https://s3.timeweb.cloud s3 cp \
  s3://skelet-backups/db/backup-2026-06-29-0300.sql.gz /tmp/restore.sql.gz
```

## 2. Восстановление в **новую** БД (рекомендуемый дрилл)

Никогда не накатывайте дамп поверх живой БД без проверки. Сначала восстанавливайте в отдельную:

```bash
# Поднять только db-контейнер
docker compose up -d db
docker compose exec db psql -U app -d postgres -c "CREATE DATABASE restore_test;"

gunzip -c /tmp/restore.sql.gz | docker compose exec -T db psql -U app -d restore_test

# Проверить counts
docker compose exec db psql -U app -d restore_test -c "
  SELECT 'users' AS table, count(*) FROM users
  UNION ALL SELECT 'posts', count(*) FROM posts
  UNION ALL SELECT 'comments', count(*) FROM comments;
"
```

Counts должны быть близки к prod (учитывая ~24h окно с момента дампа).

## 3. Восстановление поверх боевой БД

**ВНИМАНИЕ:** деструктивно. Делай только если живая БД корраптнута и/или потеряна.

```bash
docker compose stop app

# Дроп текущей БД
docker compose exec db psql -U app -d postgres -c "DROP DATABASE app;"
docker compose exec db psql -U app -d postgres -c "CREATE DATABASE app;"

gunzip -c /tmp/restore.sql.gz | docker compose exec -T db psql -U app -d app

# Применить миграции (если дамп старше последней миграции — есть шанс что они захардкодены в дампе; если нет — pnpm db:migrate)
docker compose exec app pnpm db:migrate

docker compose start app
```

## 4. Recovery drill (раз в квартал)

Минимум раз в 3 месяца:

1. Скачать последний дамп из S3
2. Восстановить в отдельную БД (`restore_test`) по §2
3. Сравнить counts с production
4. Дропнуть `restore_test`
5. Записать дату/результат в `docs/RECOVERY.md` (этот же файл, секция «История drill'ов» ниже)

## История drill'ов

| Дата | Дамп | Counts (restore vs prod) | Кто | Заметки |
|---|---|---|---|---|
| _(в первый прогон вписать)_ |  |  |  |  |
```

- [ ] **Step 19.2: Коммит**

```bash
git add docs/RECOVERY.md
git commit -m "docs(plan-6): RECOVERY.md — restore-from-backup procedure"
```

---

## Task 20: Production build smoke + docker compose локально

**Files:** — (никаких новых; финальная валидация)

- [ ] **Step 20.1: Чистая prod-сборка**

```bash
pnpm tsc --noEmit
NODE_ENV=production pnpm build
```

Ожидание: tsc чисто; build зелёный. Если build падает на отсутствии prod-env (например `DOMAIN`) — это **ожидаемое** поведение от Task 1 superRefine; запусти build с минимально валидным `.env`:

```bash
cp .env .env.bak
cat >> .env <<EOF
DOMAIN=example.ru
LETSENCRYPT_EMAIL=test@test.ru
EOF
NODE_ENV=production pnpm build
mv .env.bak .env
```

Если build всё ещё падает — расследуй сообщение.

- [ ] **Step 20.2: `docker compose build app`**

```bash
docker compose build app
```

Ожидание: образ собрался, размер ~200–400 MB (depends on standalone trimming).

- [ ] **Step 20.3: Полный compose-up локально**

```bash
docker compose up -d
docker compose ps
```

Ожидание: все 4 контейнера `Up`/`healthy`. Если `caddy` падает на отсутствии `LETSENCRYPT_EMAIL` — выставь любой email в `.env` (для локалки cert получаться не будет, но Caddy не упадёт).

Проверки:
```bash
curl -s http://localhost/api/health     # 200 + {"status":"ok","db":"ok"}
curl -s http://localhost/sitemap.xml | head -20
curl -s http://localhost/robots.txt
curl -sI http://localhost/og/<existing-slug>   # 200 + image/png
curl -s http://localhost/privacy | grep -o "<h1>[^<]*</h1>"
```

Для локального теста Caddy с самоподписанным cert: добавь `tls internal` в Caddyfile (временно), или просто открой `http://localhost` напрямую — в dev compose-сетке Caddy уже проксирует app:3000.

- [ ] **Step 20.4: Остановить compose**

```bash
docker compose down
```

- [ ] **Step 20.5: Коммит (если что-то поменялось)**

Если в процессе появились правки (например, поправлено сообщение в env, или подкорректирован Dockerfile) — коммит:

```bash
git add -A
git status
git commit -m "fix(plan-6): <конкретная-правка>"
```

Если ничего не менялось — пропустить шаг.

---

## Task 21: Recovery drill

**Files:** — (заполнение `docs/RECOVERY.md` после прогона)

- [ ] **Step 21.1: Локальный recovery drill**

Если на VPS ещё не залит — сделать локальный drill на dev-БД:

```bash
# Сгенерировать тестовый дамп локально (в роли «бэкапа»):
docker compose up -d db
docker compose exec db pg_dump -U app -d app --no-owner --no-acl --format=plain | gzip -9 > /tmp/local-drill.sql.gz

# Создать restore_test
docker compose exec db psql -U app -d postgres -c "CREATE DATABASE restore_test;"

# Восстановить
gunzip -c /tmp/local-drill.sql.gz | docker compose exec -T db psql -U app -d restore_test

# Проверить counts
docker compose exec db psql -U app -d restore_test -c "
  SELECT 'users' AS table, count(*) FROM users
  UNION ALL SELECT 'posts', count(*) FROM posts
  UNION ALL SELECT 'comments', count(*) FROM comments;
"
```

- [ ] **Step 21.2: Сравнить с counts из prod (app db) — должны совпадать**

```bash
docker compose exec db psql -U app -d app -c "
  SELECT 'users' AS table, count(*) FROM users
  UNION ALL SELECT 'posts', count(*) FROM posts
  UNION ALL SELECT 'comments', count(*) FROM comments;
"
```

- [ ] **Step 21.3: Дропнуть restore_test**

```bash
docker compose exec db psql -U app -d postgres -c "DROP DATABASE restore_test;"
rm /tmp/local-drill.sql.gz
```

- [ ] **Step 21.4: Дописать строку в `docs/RECOVERY.md` → «История drill'ов»**

Открой `docs/RECOVERY.md` и заполни первую строку таблицы:

```
| 2026-06-29 | local-drill (synthetic) | users=N, posts=M, comments=K — совпали | <твой ник> | первый прогон в plan-06 |
```

- [ ] **Step 21.5: Коммит**

```bash
git add docs/RECOVERY.md
git commit -m "docs(plan-6): record first recovery drill (local synthetic)"
```

---

## Task 22: README + retro

**Files:**
- Modify: `README.md`
- Modify: `docs/superpowers/plans/2026-06-29-plan-06-launch.md` (этот файл — секция Retro в конце)

- [ ] **Step 22.1: README — короткая launch-секция**

В README.md добавь раздел (если нет аналогичного — иначе расширь):

```markdown
## Deployment

Skelet деплоится на Timeweb Cloud VPS через docker-compose (caddy + app + db + backup). HTTPS — автоматически через Caddy + Let's Encrypt.

Подробности: [docs/DEPLOY.md](./docs/DEPLOY.md). Восстановление из бэкапа: [docs/RECOVERY.md](./docs/RECOVERY.md).

## SEO

- `sitemap.xml` и `robots.txt` — динамические (`src/app/sitemap.ts`, `src/app/robots.ts`).
- JSON-LD на публичных страницах: `BlogPosting` + `BreadcrumbList` + `WebSite` (`src/lib/jsonld.ts`).
- OG-изображения: реальная обложка поста, иначе динамика через `next/og` (`src/app/og/[slug]/route.tsx`).
- IndexNow: при `publishPost`/`updatePost`/`hidePost`/`deletePost`/`adminBanUser` пингуем `api.indexnow.org` (`src/lib/indexnow.ts`).

## Analytics

Яндекс.Метрика (`clickmap + trackLinks + accurateTrackBounce`, без webvisor) — `src/components/analytics/YandexMetrika.tsx`. Грузится только в `NODE_ENV=production` при `YANDEX_METRIKA_ID`. Cookie-баннер не делаем; в футере disclaimer + страница `/privacy`.

## Monitoring

- `/api/health` — `SELECT 1` к Postgres, 200/503.
- UptimeRobot пингует `/api/health` каждые 5 минут, алёрты в Telegram (см. `docs/DEPLOY.md` §8.5).
```

- [ ] **Step 22.2: Заполнить Retro (в этом файле, внизу)**

После выполнения всех task'ов и DoD — заполнить секцию Retro ниже (см. ## Retro).

- [ ] **Step 22.3: Финальный коммит**

```bash
git add README.md docs/superpowers/plans/2026-06-29-plan-06-launch.md
git commit -m "docs(plan-6): README launch/SEO/monitoring sections + retro"
```

---

## DoD checklist (manual e2e + автоматический)

Автоматическая часть:

- [ ] `pnpm tsc --noEmit` — чисто
- [ ] `pnpm test` — все зелёные, **+18** новых тестов прибавлено
- [ ] `NODE_ENV=production pnpm build` — зелёный
- [ ] `docker compose build app` — зелёный
- [ ] `grep -rn "R2_" src/ next.config.ts` — пусто (кроме исторических комментариев)

Манульная часть (требует браузера / dev-сервера):

- [ ] `docker compose up -d` локально → все 4 контейнера up, `/api/health` 200
- [ ] `/` → JSON-LD `WebSite` в DOM
- [ ] `/p/<published-slug>` → JSON-LD `BlogPosting` + `BreadcrumbList` + `<meta property="og:image">` со значением `coverUrl` (если есть) ИЛИ `/og/<slug>`
- [ ] `/og/<published-slug>` → 200 + image/png; `/og/non-existent` → 404
- [ ] `/sitemap.xml` → содержит ожидаемые URL'ы; не содержит draft / deleted / hidden / banned-юзеров (но содержит посты забаненных)
- [ ] `/robots.txt` → корректный, ссылка на sitemap
- [ ] `/privacy` → рендерится с контактным email
- [ ] Footer на любой публичной странице → disclaimer + ссылка на /privacy
- [ ] Метрика в production-сборке: `NODE_ENV=production YANDEX_METRIKA_ID=<тест-counter> docker compose up -d` → DevTools Network → `mc.yandex.ru/metrika/tag.js` грузится
- [ ] JSON-LD валиден через Google Rich Results Test: https://search.google.com/test/rich-results — вставить prod-URL `/p/<slug>` (после деплоя)
- [ ] Caddyfile валиден: `docker run --rm -v "$PWD/Caddyfile:/etc/caddy/Caddyfile" caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile`
- [ ] Recovery drill пройден, строка в `docs/RECOVERY.md` записана
- [ ] `docs/DEPLOY.md` написан и manual review'нут
- [ ] `docs/RECOVERY.md` написан и manual review'нут
- [ ] Retro-секция этого плана заполнена

Post-deploy (выходит за рамки plan-06 как такового, но фиксируем для трекинга):

- [ ] VPS заведён, DNS настроен, `https://example.ru` открывается с валидным cert'ом
- [ ] OAuth-приложения зарегистрированы под prod-redirect URI
- [ ] Buckets Timeweb S3 (Standard + Cold) созданы, CORS настроен, lifecycle-policy 30 дней на Cold
- [ ] IndexNow `.txt` файл лежит в `public/`, доступен по `https://example.ru/<KEY>.txt`
- [ ] Yandex.Metrika счётчик настроен, ID в `.env`, sitemap засабмичен в Webmaster
- [ ] Google Search Console настроен, sitemap засабмичен
- [ ] UptimeRobot пингует `/api/health`, Telegram-бот доставляет алёрты
- [ ] Один тестовый бэкап прошёл (`docker compose logs backup` → `[backup] uploaded:`)

---

## Retro

**Что прошло как написано:**
- Tasks 1–14 (env refactor → JSON-LD → robots → sitemap → OG → Метрика → privacy → footer) прошли по плану, TDD-цикл выдерживался; +18 тестов.
- Task 15 (Caddyfile) — security headers + www→apex + /og/* в static cache, `caddy fmt` отформатировал и `caddy validate` прошёл по парсингу (failure был только локальный CA-перм — для прод-валидации не релевантен).
- Task 16 (docker-compose) — patch применился чисто, `docker compose config` зелёный.
- Task 17 (backup scripts) — `apk add aws-cli` сработал из основного репо.
- Task 18–19 — DEPLOY.md и RECOVERY.md написаны по шаблонам плана.
- Task 21 (recovery drill) — local synthetic дамп прошёл, counts совпали (4/84/354).

**Что отклонилось от плана:**
- В Task 20 smoke обнаружились три реальных бага, не задокументированных в плане; коммит `fix(plan-6): smoke fixes`:
  1. `parseEnv` не нормализовал пустые строки → `LETSENCRYPT_EMAIL=` (placeholder в `.env`) ломал zod email-валидатор. Фикс: пустые строки маппим в `undefined` до schema.safeParse.
  2. `src/app/robots.ts` без `export const dynamic = "force-dynamic"` пытался prerender'иться при `pnpm build` (внутри Docker без env'ов) → падал на `getEnv()`. Добавили `force-dynamic` симметрично `sitemap.ts`.
  3. `scripts/backup-entrypoint.sh` использовал GNU-флаг `date -d "today 03:00"`, а alpine busybox его не понимает. Добавили `apk add coreutils` в entrypoint.
- В Task 20 локальный `docker compose up` не мог занять host-порт 5432 (у пользователя другой postgres рядом). Workaround: docker-compose.smoke.yml с `ports: !reset []` (в репозиторий не коммитили, описано в этом retro).

**Сюрпризы / undocumented gotcha'и:**
- `docker compose restart` не перечитывает `env_file: .env` — нужно `up --force-recreate <service>`. Это бьёт по локальному smoke-циклу; в prod-операциях deploy через `up -d --build app` уже делает recreate.
- `apk add aws-cli` в Alpine 3.20+ работает напрямую — fallback через pip из плана не понадобился.
- typedRoutes в Next.js 15 кэширует `.next/types/routes.d.ts` между билдами — после добавления `/privacy` потребовался полный `pnpm build` чтобы tsc снова прошёл.
- Caddy `auto_https` в локалке (DOMAIN=localhost) делает self-signed cert, поэтому smoke использовал `https://` с `-k`.

**Маркеры на Phase 2 / далее:**
- IndexNow verification (`public/<KEY>.txt`) — пока ручной шаг в DEPLOY.md §8.1. Можно автогенерить в `scripts/entrypoint.sh` из `INDEXNOW_KEY` (Phase 2).
- `parseEnv` empty-string handling уместен — но для долгосрочной гигиены добавить тест («`KEY=` приравнивается к unset»).
- Recovery drill сейчас локальный/synthetic. Первый prod-drill — после деплоя на VPS (записать в `docs/RECOVERY.md` строку с реальным дампом из S3 Cold).
- `Caddyfile` для preview-окружений (staging.example.ru) не предусмотрен — потребуется при появлении staging.
- `/og/[slug]` рендерится Node-runtime; если будет нужен edge-runtime — переход не тривиален (font loading, кеш).

---
