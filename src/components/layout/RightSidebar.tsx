export function RightSidebar({ className }: { className?: string }) {
  return (
    <aside className={className} aria-label="Дополнительно">
      {/* Структуру layout зафиксировали в plan-5a, наполнение — отдельная итерация. */}
    </aside>
  );
}
